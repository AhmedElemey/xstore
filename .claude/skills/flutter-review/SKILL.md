---
name: flutter-review
description: Team-lead review checklist and accumulated lessons for xStore Flutter code. Use BEFORE writing or modifying any Dart code in this repo, and AFTER completing a change to self-review it. Also use when a review finding or user correction produces a new lesson to record.
---

# xStore Flutter Review — Team Lead Skill

Two jobs:
1. **Before writing Dart code**: read the Lessons Learned log below and apply every relevant lesson.
2. **After a review finding or user correction**: record the lesson (see "Recording a lesson").

The base rules (disposal, Riverpod lifecycles, mounted checks, rebuild storms, over-engineering bans) live in the project CLAUDE.md — this skill holds what we learn on top of them.

## Recording a lesson

Append to the Lessons Learned log whenever any of these happen:
- The review hook or your own self-review catches a real issue in your change.
- The user corrects your approach, style, or a design decision.
- You discover a repo-specific pattern the hard way (an API quirk, a widget that must be used a certain way, a backend contract detail).

Format — one entry, newest last:

```
### YYYY-MM-DD — <short title>
- **What happened:** <the mistake or correction, one sentence>
- **Rule:** <the generalized do/don't to apply next time, one or two sentences>
- **Where it applies:** <file/feature/pattern scope>
```

Rules for the log:
- Generalize: record the *rule*, not the incident. "Dispose PageControllers in carousel widgets" not "fixed banner_carousel.dart".
- No duplicates: if a lesson already covers it, sharpen that entry instead of adding a new one.
- Keep entries short — this whole file gets loaded into context; prune entries that graduate into CLAUDE.md hard rules.
- If a lesson is important enough to be a hard requirement for every change, promote it into CLAUDE.md's rules section and remove it from here.

## Lessons Learned

<!-- Newest entries at the bottom. Do not delete this section; append below. -->

### 2026-07-23 — UserImage and StoreImage are separate on update-profile
- **What happened:** Profile avatar picker was wired to `StoreImage`/`storeImageUrl`, conflating the user's profile photo with the vendor store logo.
- **Rule:** `UpdateProfileRequest` carries both pairs: `userImageUrl`/`userImagePath` → `UserImage`/`userImageUrl` (profile avatar picker, removal clears user only) and `storeImageUrl`/`storeImagePath` → `StoreImage`/`storeImageUrl` (store logo). Always send both URL keys on the wire; attach each file under its own multipart field name.
- **Where it applies:** `update_profile_request.dart`, `profile_state.dart` `toUpdateProfileRequest()`, `profile_remote_datasource.dart` `updateProfileFormData`.

- **What happened:** Save still routed through `UserEntity.toEditedUser()` and a separate avatar-upload call instead of the confirmed `UpdateProfileRequest` DTO with inline `StoreImage` multipart.
- **Rule:** Profile edit builds `UpdateProfileRequest` from `ProfileState.toUpdateProfileRequest()`; repository/usecase/datasource accept that type; wire via `updateProfileFormData` (`storeImage` file + camelCase fields). Drop the pre-save avatar upload — one multipart PUT handles image + fields.
- **Where it applies:** `update_profile_request.dart`, `profile_state.dart`, `profile_provider.dart` `saveProfile`, `profile_remote_datasource.dart`.

- **What happened:** Edit-profile save still sent legacy keys (`avatarUrl`, `latitude`, `email`, `bio`) instead of the confirmed `UpdateProfileRequest` shape (`storeImageUrl`, `lat`/`lng`, `detailedAddressByGoogleMaps`, `cityId`, date-only `birthDate`).
- **Rule:** Build update-profile bodies from the C# `UpdateProfileRequest` contract — omit commented-out email/phone; map entity location fields to `detailedAddressByGoogleMaps`/`detailedAddressByUser`/`cityByGoogleMaps`/`governmentByGoogleMaps`; wire IDs as `cityId`/`governorateId` (C# `GovernorateId`, not `governmentId`); send `storeImageUrl` (always, null to clear); format `birthDate` as `YYYY-MM-DD`. Populate `editLocation` on GPS detect for the Google address field.
- **Where it applies:** `profile_state.dart` `toUpdateProfileRequest()`, `update_profile_request.dart`, `profile_remote_datasource.dart` `updateProfileWireFields`/`updateProfileFormData`, `profile_provider.dart` `saveProfile`.

- **What happened:** Live `POST /api/auth/google/consumer/login` returned 401 `"Invalid Google identity token."` even though Google sign-in succeeded — `GoogleSignIn()` had no `serverClientId`, so the ID token's audience was the Android/iOS OAuth client, not the Web client the backend verifies.
- **Rule:** When exchanging a Google ID token with a backend, always construct `GoogleSignIn(serverClientId: DefaultFirebaseOptions.googleWebClientId)` (the OAuth Web client from `google-services.json`, client_type 3). Send `googleAuth.idToken` to the role-specific endpoint — never the Firebase ID token.
- **Where it applies:** `social_auth_datasource.dart`, any future Google Sign-In wiring.

### 2026-07-23 — Google backend login sends clientId in POST body
- **What happened:** Backend Google login needed the OAuth Web client ID in the request payload, not only as the ID token audience via `serverClientId`.
- **Rule:** `loginWithGoogle` must POST both `idToken` and `clientId` (same value as `DefaultFirebaseOptions.googleWebClientId` / `GoogleSignIn.serverClientId`) so token minting and backend verification stay aligned.
- **Where it applies:** `auth_remote_datasource.dart` `loginWithGoogle`, `firebase_options.dart` `googleWebClientId`.

### 2026-07-23 — FCM sync must not read authProvider inside Auth.build() (corrected 2026-08-27)
- **What happened:** Cold start with no persisted session called `syncFcmDeviceTokenWithBackend(ref, user: null)` from inside `Auth.build()`; line 28 synchronously `ref.read(authProvider)` while auth was still building → Riverpod self-dependency assert. Same class of bug as the prefetch lesson — `Right(null)` restore still entered the `firstRestore` block. The original fix here (deferring the read with `Future(() => ref.read(authProvider)...)`) was itself wrong: `Auth.setUser()` and `Auth.adoptSession()` called `syncFcmDeviceTokenWithBackend(ref)`/`syncDeliveryBackendSession(ref)` with no `user`, relying on that same deferred fallback, and it asserted every time (caught 2026-08-27 by running `flutter test --dart-define=MOCK=true`, which the "Requires --dart-define=MOCK=true" skips normally hide from a plain `flutter test`).
- **Rule:** Riverpod's self-dependency assert (`listenable._origin != origin`) is keyed on the **element/ref identity**, not call-stack timing — `ref.read(authProvider)` throws through `Auth`'s own `ref` whether it's synchronous inside `build()` or deferred with `Future(() => ...)` in a later microtask/event. There is no timing trick that makes a provider's own `ref` able to read itself. The only fix is to never read auth through `Auth`'s own `ref` at all: every method on `Auth` (`build`, `setUser`, `adoptSession`, and any future one) already has `user` in scope — always pass `user: user` explicitly to `syncFcmDeviceTokenWithBackend`/`syncDeliveryBackendSession`/`prefetchProfileData`, with no fallback read. Verify this class of fix with `flutter test --dart-define=MOCK=true`, not just the default `flutter test` — the affected tests are gated behind `skip: MockConfig.useMock` and silently no-op in the default run, so a regression here only surfaces in the MOCK=true CI job.
- **Where it applies:** `auth_provider.dart` (`Auth.build`/`setUser`/`adoptSession`), `fcm_device_token_sync_provider.dart`, `delivery_backend_session.dart`, `profile_provider.dart` `prefetchProfileData`, and any future side-effect fired from inside a notifier that needs that notifier's own async state.

### 2026-07-23 — Notification wire enums are int ordinals, not strings
- **What happened:** Live seed-account notifications send `type`/`priority` as JSON integers; the parser assumed camelCase enum name strings and silently mapped every item to `systemAnnouncement`/`normal`. Ordinal guessing was rejected — safe fallback until the C# enum declaration is confirmed.
- **Rule:** For backend enums, never infer int→member mapping from seed data; handle numeric wire values with an explicit fallback + comment, and use type-checked `_optString`/`_optInt`/`_optDouble` instead of `as` casts in notification (and similar) parsers. Skip malformed list items per-row so one bad row doesn't empty the feed.
- **Where it applies:** `notifications_remote_datasource.dart` and any future wire-enum parsing.

- **What happened:** Learning loop created; no incidents recorded yet.
- **Rule:** Every review finding or user correction must be appended here before the task is considered complete.
- **Where it applies:** All Dart code in this repo.

### 2026-07-11 — Domain logic in widget files
- **What happened:** `commissionStatusForOrder` (order status → commission status mapping) was defined in `order_price_breakdown.dart` (a widget file) and imported by `vendor_commission_wallet_provider.dart`, creating a provider→widget dependency.
- **Rule:** Pure mapping/business functions go in the feature's `domain/` layer, never in presentation widget files. Providers must never import widget files.
- **Where it applies:** All features; watch for it when a helper function is "convenient" to co-locate with the widget that first used it.

### 2026-07-11 — Financial aggregates capped by page size
- **What happened:** The vendor commission wallet sums fees over a single fetch of 200 orders; a vendor with more due orders gets a silently understated outstanding balance, which gates listing publishes.
- **Rule:** Never compute a money/balance figure from one page of a paginated endpoint. Either loop pages until exhausted, or detect `results == pageSize` and surface that the figure is a lower bound; push for a backend aggregate endpoint for anything financial.
- **Where it applies:** Commission wallet, any future totals/earnings/analytics computed client-side.

### 2026-07-11 — State writes after await in Riverpod notifiers
- **What happened:** Auth flow notifiers (e.g. `LoginNotifier.login`) assign `state = ...` after `await` with no disposal guard; if the user navigates away mid-request, the autoDispose notifier is disposed and the assignment throws an unhandled StateError.
- **Rule:** Guard post-await state writes. In `@riverpod` codegen Notifier classes: `_disposed` flag reset in `build()` + `ref.onDispose(() => _disposed = true)`. In `StateNotifier` subclasses: use the built-in `mounted` getter (state_notifier 1.0.0, line 152 — it exists, despite it being commonly believed to be State-only). Caveat for keepAlive notifiers cleared via `ref.invalidate`: riverpod 2.x reuses the same notifier instance across rebuilds (verified empirically), so a `_disposed` flag reset in `build()` reopens the gate for a fetch still in flight — use a monotonic epoch bumped in `ref.onDispose` and compared per call instead (see `ProfileNotifier._sessionEpoch`).
- **Where it applies:** Every async method in `@riverpod` Notifier and `StateNotifier` classes that writes `state` after an await. 2026-07-18: audit found only auth/profile hardened; product_detail, product_reviews, explore, checkout, order_detail, listing_form, my_listings, vendor_orders, vendor_order_detail were then guarded the same way — apply the pattern to every NEW notifier from day one.

### 2026-07-11 — StateNotifier with Timer needs a dispose override
- **What happened:** `PhoneAuthNotifier` (StateNotifier) started a `Timer.periodic` resend cooldown but had no `dispose()` override; a disposal during the 60s cooldown would leave the timer firing into a disposed notifier.
- **Rule:** Any `StateNotifier` owning a `Timer`/`StreamSubscription` must override `dispose()` to cancel it, and long-lived periodic callbacks must check `mounted` before writing state.
- **Where it applies:** `phone_auth_provider.dart` and any StateNotifier-based provider (legacy pattern in this repo alongside codegen `@riverpod`).

### 2026-07-11 — Route role-classification must be verified against call sites
- **What happened:** While adding role guards, `/order/:id` looked consumer-only but vendors also open it (`/incoming-orders` → OrdersScreen → OrderCard); restricting it would have broken the vendor incoming-orders flow.
- **Rule:** Before classifying a route as role-restricted, `rg` every `context.push`/`go` call site for it and trace which role reaches each one. Shared routes in this repo: `/order/:id`, `/product/*`, `/seller/*`, `/profile*`, notifications, info screens. Guards live centrally in `computeXStoreAuthRedirect` + `isVendorRestrictedRoute`/`isConsumerRestrictedRoute` (app_routes.dart) — extend those, never scatter per-screen checks.
- **Where it applies:** Any new route or role-gated feature touching `lib/core/router/`.

### 2026-07-11 — Guest mode: three enforcement layers, one flag
- **What happened:** Guest mode was added as a persisted `guestModeProvider` flag with enforcement in exactly three places: `isGuestAccessibleRoute` + `computeXStoreAuthRedirect` (routes), and `requireLogin(context, ref)` in `shared/utils/` (inline actions on browsable screens).
- **Rule:** Account-gated behavior goes through `requireLogin` — never hand-roll `authProvider.valueOrNull == null` checks at action sites. It shows a decision dialog (Sign in / Not now) and returns false synchronously; the caller just aborts. New browse-safe routes must be added to `isGuestAccessibleRoute` or guests get bounced to login. Prefs-backed flags needed by the router at cold start must be synced synchronously in splash (`enable()` before `context.go`) — the provider's lazy prefs load races the first redirect otherwise.
- **Where it applies:** Any new user-triggered action (cart, wishlist, reviews, chat, checkout) and any new route added to the router.

### 2026-07-11 — The route redirect is a safety net, not the guest UX
- **What happened:** Guest gating was wired into inline actions only; tapping nav tabs (wishlist/orders/profile), the home cart icon, or help-screen shortcuts hit the silent route redirect and dumped guests on the login screen with no dialog — user reported "the pop up is not showing".
- **Rule:** Every user-visible entry point to an account-bound route must call `requireLogin` at the tap site (bottom-nav `_onTap`, header icons, CTA buttons on guest-browsable screens). The `computeXStoreAuthRedirect` guard only backstops deep links and programmatic navigation. When adding a gated route, grep for every widget that navigates to it and gate each tap.
- **Where it applies:** `xstore_bottom_nav.dart`, any header/app-bar action icons, CTA buttons on guest-accessible screens (help/info), and every future navigation into cart/orders/profile/wishlist/notifications.

### 2026-07-11 — Lazy async providers read as Loading in widget tests
- **What happened:** A widget test tapped a button whose handler did `ref.read(authProvider).valueOrNull` — the first-ever read of the lazy async provider returned AsyncLoading, so a signed-in fake user read as null and the test failed.
- **Rule:** In widget tests exercising handlers that `ref.read` an async provider, warm the provider up in the harness (`ref.watch` it in the tree, then `pumpAndSettle`) so tap-time reads see AsyncData — mirroring the real app where splash awaits auth first.
- **Where it applies:** All widget tests around auth-gated actions or any FutureProvider/async-notifier read inside callbacks.

### 2026-07-11 — Live backend is the default; mock is opt-in
- **What happened:** The repo flipped from mock-first (MOCK defaulted true, placeholder API URLs) to live-first: `MockConfig` defaults MOCK=false, and `ApiEndpoints.baseUrl` falls back to the hosted backend (HTTPS verified; 401 without the Basic license key, real data with it). The specific `tempurl.com` host/key rotate over time as the free trial expires — see the 2026-08-04 "tempurl.com free-trial host rotates" lesson for the current values and where they're wired.
- **Rule:** Assume every run/build/test hits the live backend unless `--dart-define=MOCK=true` is passed. When adding datasource code, the real API path is the primary path — mock branches are legacy fixtures. Tests must keep their `skip: MockConfig.useMock ...` guards so both CI modes stay green.
- **Where it applies:** All datasources, Taskfile/CI defines, and anything reading `MockConfig.useMock`.

### 2026-07-11 — Backend enums are 1-based C#; 0 means unset, not the first member
- **What happened:** The app mapped listing conditions 0-based (0=New…) from a "confirmed by live probe" comment, but the probe data all had `condition: 0` — the C# default for an unset field, not a member. The real contract is `ListingCondition { New=1, LikeNew=2, Good=3, UsedForParts=4 }`, so every parsed condition was off by one and the app offered 5 seller options against 4 backend values (lossy round-trip).
- **Rule:** Never infer enum wire codes from seed data — ask for the C# enum declaration. Treat `0` as unset/null for any backend enum. Keep app enums 1:1 with backend enums (no app-only members). Condition mapping is centralized in `listing_model.dart` (`listingConditionFromToken`/`ToWire`/`Label*`) with a contract test in `test/listing_condition_wire_test.dart` — extend those, don't add local switches.
- **Where it applies:** All wire-enum handling (condition, listing status, order status, payment method) in `data/models`.

### 2026-07-11 — This app cannot run on Flutter web; verify flows with widget tests
- **What happened:** Attempting browser verification of the guest sheet hit a permanent black screen: `firebase_options.dart` throws `UnsupportedError` for web inside `bootstrap()` before `runApp`, so the web build never renders a frame.
- **Rule:** Don't burn time on `flutter run -d chrome/web-server` for this repo until web Firebase options exist. Prove UI flows with widget tests (GoRouter stub harness in `test/require_login_test.dart` is the template). When a user reports old behavior that the current code demonstrably gates, suspect a stale build first and say so — hot reload doesn't always re-run existing widgets; ask for hot restart or a rebuild.
- **Where it applies:** All manual verification attempts; any future "works in code, user sees old behavior" reports.

### 2026-07-11 — Fixed-height Columns in bottom sheets overflow
- **What happened:** The sign-in sheet's Column (icon + title + body + two buttons) overflowed by 14px inside a default `showModalBottomSheet` height constraint — caught only because a widget test rendered it in a 600px-tall viewport.
- **Rule:** Bottom-sheet content must be wrapped in `SingleChildScrollView` (or use `isScrollControlled` with explicit constraints) — never rely on the content fitting the sheet's max height. Widget-test every new sheet/dialog; the test viewport catches overflows real phones might hide.
- **Where it applies:** All `showModalBottomSheet`/`showDialog` content in `shared/` and feature widgets.

### 2026-07-11 — Custom validateStatus can silently disable token refresh
- **What happened:** `LegacyRouteOptions.allowNotFound` used `validateStatus: status < 500`, which accepted 401/403 as normal responses — `TokenRefreshInterceptor` only acts in `onError`, so expired sessions on legacy routes would never refresh and would render as "empty data" instead.
- **Rule:** Any per-request `validateStatus` override must keep 401/403 as errors. Widen acceptance only for the specific status being tolerated (e.g. `2xx || 404`), never with a blanket `< 500`.
- **Where it applies:** All Dio `Options(validateStatus: ...)` overrides; currently `legacy_route_options.dart` used by orders, store-hours, and profile datasources.

### 2026-07-11 — Verify review-hook claims against package source
- **What happened:** The automated review hook flagged `if (!mounted)` in a StateNotifier as a runtime error, claiming StateNotifier has no `mounted`; checking `~/.pub-cache/.../state_notifier.dart` proved the property exists (state_notifier 1.0.0 line 152) and the flag was a false positive. It recurred 2026-07-18, plus a second misread: the hook called `if (!mounted) return result.isRight();` "inconsistent" while every sibling method used the identical pattern — it only saw the partial edit. Recurred again 2026-08-06: the hook flagged a `TextEditingController` created in a `showDialog` helper (`my_package_requests_screen.dart` `_cancelRequest`) as "never disposed," but the edit already called `reasonCtrl.dispose()` unconditionally right after the dialog resolved, before any early return — re-reading the file confirmed the dispose call was present exactly where the pattern requires it. Recurred a third time the same day (2026-08-06), 3x in one sitting while wiring `order_status_changed` analytics tracking into `vendor_orders_provider.dart`/`orders_provider.dart`: the hook repeatedly claimed a method parameter (`reason`, `orderId`) was "not in scope" inside a `result.fold((failure) {...}, (order) { ...HERE... })` success closure nested inside that same method — it's a completely ordinary Dart closure capturing its enclosing method's parameters, not an out-of-scope reference. Each time, re-reading the file confirmed the closure was lexically inside the method whose parameter it used.
- **Rule:** When a review claim is about an API's existence/signature, verify against the package source in the pub cache; when it claims inconsistency, bypassed logic, missing disposal, or a variable/parameter "not in scope" inside a nested closure, re-read the full current file — hooks judge partial snippets (`tool_input.content`/`new_string`) and can miss a dispose call, or the enclosing method signature, that's present just outside the shown diff. A closure literal (`(x) { ... }`) passed as a callback always captures its lexically-enclosing method's parameters and locals — that is never a scope error on its own; only flag scope issues you can point to a concrete undefined identifier for. Only rewrite after the claim survives verification against the actual file.
- **Where it applies:** All hook/reviewer feedback that asserts framework/package API facts, cross-method consistency, resource-disposal presence, or closure/variable-scope errors.

### 2026-08-06 — A "vendor confirms order" action can have two independent provider stacks behind two independent screens
- **What happened:** Adding a required `DeliveryMethod` parameter to `OrdersRepository.confirmOrder` (so the vendor picks self vs. platform delivery when accepting an order) started from `vendor_orders_provider.dart`/`vendor_order_detail_provider.dart` (the vendor-only `VendorOrdersScreen`/`VendorOrderDetailScreen`). `flutter analyze` then surfaced two more real call sites the initial pass missed entirely: `orders_provider.dart`'s `OrdersNotifier.confirmOrderVendor()` (used by the role-shared `OrderCard` widget, rendered from both `OrdersScreen` — consumer's own orders — and the vendor's incoming-orders view) and `order_detail_provider.dart`'s `OrderDetailNotifier.confirmOrderVendor()` (used by the role-shared `OrderActionButtons`, rendered from the generic `OrderDetailScreen`). Two conceptually-identical vendor actions ("confirm this order"), four screens, two entirely separate provider/notifier stacks with duplicated logic — this repo's Orders feature has a role-dedicated screen pair AND a shared consumer+vendor screen pair doing the same thing, matching the shape already documented in the "Route role-classification must be verified against call sites" and "'Go to Home after auth' has 9 call sites" lessons.
- **Rule:** Before declaring a signature/behavior change to a shared-action method (confirm/reject/ship/etc.) complete, `rg` the method name across the WHOLE `lib/` tree, not just the screen you started from — a "vendor-only" action in this codebase routinely has a second, parallel implementation behind a role-shared widget (`OrderCard`, `OrderActionButtons`) that a feature-scoped grep won't surface until the compiler complains. Let `flutter analyze` run to completion and fix every resulting error before considering the change done — each one is a real caller, not noise.
- **Where it applies:** `orders_provider.dart`, `order_detail_provider.dart`, `vendor_orders_provider.dart`, `vendor_order_detail_provider.dart`, and any future change to a method shared between the role-dedicated and role-shared order screens.

### 2026-07-11 — Surface API error bodies on 401
- **What happened:** Login 401 from `{"error":"Invalid email or password."}` was mapped to Dio's generic status-code boilerplate in the UI because `mapDioException` only read `e.message`, not the response body.
- **Rule:** For `badResponse`, always try `_serverErrorMessage` (`error`, `message`, nested `error.message`) before falling back to Dio's message — especially on 401/403 where the backend sends actionable text.
- **Where it applies:** `dio_error_mapper.dart` and any new error-mapping code.

### 2026-07-11 — Self-referencing implements clause
- **What happened:** While rewriting the wishlist datasource header, the impl class was declared as `class WishlistRemoteDataSourceImpl implements WishlistRemoteDataSourceImpl` — implementing itself instead of the abstract interface, a compile error.
- **Rule:** When editing a `class XImpl implements X` declaration, re-read the `implements` target after the edit; the Impl/interface name pair differs by only a suffix and is easy to autocomplete wrong.
- **Where it applies:** All datasource/repository interface+impl pairs in `data/` layers.

### 2026-07-11 — Legacy store-hours 404 until backend ships
- **What happened:** `GET /vendors/{id}/store-hours` returns 404 on the hosted backend (no `/api` equivalent yet); profile load logged errors and store-open badge stayed broken.
- **Rule:** Undeployed legacy modules (`/vendors/*/store-hours`, `/users/*/store`, `/orders/*`) get 404 fallbacks at the datasource: reads return sensible defaults, writes/toggles persist in the in-memory cache for the session. Use `LegacyRouteOptions.allowNotFound()` so Dio does not throw (and the debug logger stays quiet). Skip vendor-only fetches when `user.role != UserRole.vendor`.
- **Where it applies:** `legacy_route_options.dart`, `store_hours_datasource.dart`, `profile_remote_datasource.dart`, `orders_remote_datasource.dart`.

### 2026-07-11 — Dialog dismiss must use dialog context
- **What happened:** "Confirm all pending" on `VendorOrdersScreen` called `Navigator.pop(context)` with the screen's `context` inside a `showDialog` builder that discarded `dialogContext`; go_router popped the shell branch instead of the overlay, emptying the stack.
- **Rule:** In `showDialog`/`showModalBottomSheet` builders, always `Navigator.pop(dialogContext)` (or `Navigator.of(dialogContext).pop()`) — never the parent screen `context`. Follow `require_login.dart` / `order_card.dart`.
- **Where it applies:** Any overlay (dialog, bottom sheet) inside a `StatefulShellRoute` tab or other go_router-managed navigator.

### 2026-07-11 — No postFrameCallback fetch on empty list state
- **What happened:** `ProfileMenuBlocks` called `fetchOrders()` in a `postFrameCallback` whenever `totalCount == 0` — after a failed or empty fetch, every rebuild re-fired the request, hammering `/orders/vendor/{id}` in a loop.
- **Rule:** Never tie a fetch side-effect to "list is empty" without a one-shot guard (`hasLoaded`, error latch, or fetch only from the destination screen). Profile badges read existing provider state; they don't prefetch.
- **Where it applies:** Profile widgets, any `build()`-time `addPostFrameCallback` that triggers network calls.

### 2026-07-11 — Paginated list endpoints use 1-based page
- **What happened:** `allCities` / `allGovernments` / `allStoreCategories` called the live API with `page=0`, which returns HTTP 500; `page=1` (or omitting `page`) returns 200.
- **Rule:** The xStoreEcommerce `/api/*` list endpoints use **1-based** `page`. Never send `page=0` on the wire — use `page: 1` for the first page, or translate 0-based notifier state at the datasource boundary (see `notifications_remote_datasource.dart`: `page + 1`).
- **Where it applies:** Cities, governments, store categories, listings, reviews, notifications — any paginated GET.

### 2026-07-11 — Listing write payload: flat body, int condition
- **What happened:** Earlier probes concluded POST/PUT needed a `{"command": {...}}` wrapper; that wrapper passes validation but EF SaveChanges 500s. The user's working Postman example is a flat body — but `"condition": "New"` (string) still 400s; `"condition": 0` (int) returns 201. Flat + int condition + string/null `attributes` is the live contract.
- **Rule:** POST/PUT `/api/listings` send fields at the root — never wrap in `command`. Wire `condition`/`status` as integer codes via `listingConditionToWire` / `listingStatusToWire`. Wire `attributes` as a JSON string (`""`, `jsonEncode(map)`, or null) — never a JSON object. Probe both flat and wrapped shapes before blaming backend SaveChanges.
- **Where it applies:** `listing_remote_datasource.dart`, `listing_model.dart` wire helpers.

### 2026-07-11 — Listing write payload uses command wrapper
- **What happened:** `POST /api/listings` returned 400: `command` required, `condition` rejected as `"LikeNew"` string, `attributes` rejected as `{}` object.
- **Rule:** ~~Confirmed live contract for create/update: wrap fields in `{"command": {...}}`~~ **Superseded** by the flat-body lesson above (2026-07-11 same day). Keep integer `condition`/`status` and string `attributes` — drop the wrapper.
- **Where it applies:** `listing_remote_datasource.dart`, `listing_model.dart` wire helpers (`listingConditionToWire`, `listingStatusToWire`).

### 2026-07-11 — Parse formatted money with Validators.parseMoneyInput
- **What happened:** Add-listing commission preview used `double.tryParse` on `form.priceInput` after `_formatPriceInput` inserted commas (`1,500`); parse failed above 999 so price read as null and the breakdown hid.
- **Rule:** Any UI reading `priceInput` / `compareAtPriceInput` / `shippingCostInput` from listing form state must use `Validators.parseMoneyInput` (strips commas) — never raw `double.tryParse`.
- **Where it applies:** `add_listing_screen.dart`, any widget displaying derived values from formatted money fields.

### 2026-07-11 — Never push a StatefulShellRoute branch from outside the shell
- **What happened:** After publish, Navigator threw `!keyReservation.contains(key)` (duplicate page keys). `/listing/add` lives in a shell branch; `MyListingsScreen` used `context.push('/listing/add')` from the overlay route `/listing/my`, stacking the same branch route twice. The publish flow also double-navigated (`go` with `?msg=published` then `go` again to strip the query).
- **Rule:** Routes registered under `StatefulShellRoute` branches are reached with `context.go` (tab switch), never `context.push` from sibling/overlay routes. One navigation per success — show snackbar then `go` once; don't pass toast state via query params that trigger a second `go` on the destination screen.
- **Where it applies:** `my_listings_screen.dart`, `add_listing_screen.dart`, any future shell-tab route (`listingAdd`, home/explore tabs).

### 2026-07-11 — POST /api/listings 500 was the command wrapper, not EF
- **What happened:** Create listing 500s were attributed to a backend EF bug; re-probing showed the `command` wrapper 500s while the flat Postman body (with int `condition`) returns 201. The earlier "backend gap" diagnosis was wrong for the current API.
- **Rule:** Before mapping opaque SaveChanges 500s to `listingPublishServerError`, diff the app's write shape against a known-good flat probe — wrapper vs flat is a common false positive. Keep the user-facing server-error mapping for genuine 500s after the payload shape is confirmed.
- **Where it applies:** `listing_remote_datasource.dart`, `listing_form_notifier.dart`, `dio_error_mapper.dart`.

### 2026-07-11 — Profile update-profile wire keys are asymmetric
- **What happened:** Edit Profile save used a partial PUT body and skipped `avatarUrl` when null, so avatar removal never reached the server; GET used `birthDate`/`whatsAppNumber`/`instagramPage` while read parsing only looked for `dateOfBirth`/`whatsappNumber`/`instagramHandle`.
- **Rule:** Derive update-profile PUT keys from CONFIRMED get-profile / `UserModel.fromJson` field names; keep documented write-only aliases (`whatsAppNumber`, `instagramPage`); always send `avatarUrl` (null to clear); add `optString` fallbacks in `fromJson` when read/write keys differ.
- **Where it applies:** `profile_remote_datasource.dart`, `user_model.dart`, `profile_state.dart` `toEditedUser()`.

### 2026-07-11 — Bilingual store name: parse vs display
- **What happened:** `UserModel.fromJson` collapsed `storeName` with legacy-first precedence and treated `""` as present, so localized keys were skipped; mock get-profile dropped lat/lng on fetch after save.
- **Rule:** Match `UserEntity.displayName(isArabic)`: add `displayStoreName(isArabic)` for runtime locale; in `fromJson` prefer `storeNameEn`/`storeNameAr` before legacy `storeName`; `optString` treats blank/whitespace as absent; mock get/update share one `_mockUserModelFromEntity` so save→fetch round-trips all profile fields.
- **Where it applies:** `user_model.dart`, `user_entity.dart`, `profile_remote_datasource.dart`, profile wire tests.

### 2026-07-11 — get-profile response wrapper
- **What happened:** Live GET/PUT `/api/auth/get-profile` returns `{"user":{...},"store":...}` but `ProfileRemoteDataSource.getProfile` parsed the top-level map as a user; login/register return `{token, refreshToken}` only — no user object.
- **Rule:** Unwrap profile responses through shared `parseProfileUserJson` (wrapper first, raw user map as safety net); never assume login and get-profile share the same shape; verify with a live probe before fixing parsers.
- **Where it applies:** `user_model.dart`, `auth_remote_datasource.dart`, `profile_remote_datasource.dart`.

### 2026-07-11 — Prefetch enriched profile after auth
- **What happened:** `profileNotifierProvider` stayed empty until ProfileScreen mounted, so stats/vendor enrichment lagged behind `authProvider` even though login already called get-profile for identity.
- **Rule:** After session adopt/restore, fire-and-forget `prefetchProfileData(ref)` → `ProfileNotifier.refreshProfileData()`; clear with `resetProfileData(ref)` on logout; one shared refresh method for prefetch, save, and pull-to-refresh — flag redundant on-mount fetches before removing.
- **Where it applies:** `profile_provider.dart`, `auth_provider.dart`, profile screens.

### 2026-07-11 — Reset enriched profile on forced 401 logout
- **What happened:** Token-refresh failure in `dio_provider` cleared auth storage and invalidated `authProvider` but left `profileNotifierProvider` warm with the previous user's stats until explicit logout.
- **Rule:** Any forced session clear (401 refresh failed, logout, delete account) must call `resetProfileData(ref)` alongside `ref.invalidate(authProvider)`; `TokenRefreshInterceptor.onRefreshFailed` only — not on retried 401s that refresh succeeds.
- **Where it applies:** `dio_provider.dart`, `auth_provider.dart`, `profile_provider.dart`.

### 2026-07-11 — Side-effects fired from inside build() see the provider as Loading
- **What happened:** `Auth.build()`'s restore fold called `prefetchProfileData(ref)` before `build()` returned; `refreshProfileData` immediately did `ref.read(authProvider).valueOrNull`, which on a first build is `AsyncLoading` with no previous value → null user → the prefetch reset profile state instead of loading it (proven with a minimal Riverpod repro). The existing prefetch test only covered `adoptSession`, which sets `state` before prefetching, so it stayed green.
- **Rule:** Never fire, from inside a provider's `build()`, a side-effect that synchronously reads that same provider's async state — on first build `valueOrNull` is null (invalidate-rebuilds keep the previous value, masking the bug). Either pass the value explicitly into the side-effect or defer it past build completion (`Future(() => ...)`). For first-build-only side-effects, a notifier instance field works: riverpod 2.x reuses the instance across `ref.invalidate` rebuilds (see `Auth._restoredOnce`). Test the cold-start/restore path, not just the adopt path.
- **Where it applies:** `auth_provider.dart` prefetch wiring; any cross-provider side-effect launched from a `build()` method.

### 2026-07-12 — Epoch guard on every await in keepAlive notifiers
- **What happened:** `ProfileNotifier._sessionEpoch` guarded only `refreshProfileData()`; `saveProfile()` had multiple post-await `state =` writes and could repopulate profile state after `resetProfileData` mid-save.
- **Rule:** In keepAlive notifiers using `_sessionEpoch`, capture `epoch` at method entry and compare after **every** `await` before any `state` write or `ref.invalidate` — including multi-step flows (`saveProfile`, not just single-fetch methods). Add a mid-invalidate race test per flow.
- **Where it applies:** `ProfileNotifier` and any keepAlive `@Riverpod` notifier cleared via `ref.invalidate`.

### 2026-07-12 — No mount-time refresh when auth prefetch already runs
- **What happened:** `ProfileScreen` called `refreshProfileData()` on mount while cold-start/adopt prefetch already loaded the same data — redundant double-fetch on tab open.
- **Rule:** One shared refresh entry point per resource; if auth adopt/restore prefetches enriched profile, screens read `profileNotifierProvider` and rely on pull-to-refresh for explicit reload — don't re-fetch on mount. Verify `isLoading && profile == null` still shows skeleton while prefetch is in flight.
- **Where it applies:** `profile_screen.dart`, any tab that mirrors a global prefetch.

### 2026-07-15 — Profile tab: don't block on enriched-profile fetch failure when auth has identity
- **What happened:** Login/restore already hydrates `authProvider` via get-profile, but a failed `profileNotifierProvider` prefetch showed a full-page `ErrorStateWidget` — the Profile tab looked broken even though the session user was available.
- **Rule:** Full-page profile error only when auth has no identity (`user.id.isEmpty` stub). Otherwise render the tab from `authProvider` and show the existing inline retry banner for enriched-profile failures. On mount, call `refreshProfileData()` only when `profile == null && !isLoading && error == null` (prefetch missed) — never duplicate an in-flight prefetch.
- **Where it applies:** `profile_screen.dart`, `profile_provider.dart` `refreshProfileData()`.

### 2026-07-13 — Phone-first auth: confirmed backend contract
- **What happened:** Switching auth to phone+password, live probes confirmed: consumer/vendor register succeed with the `email` key omitted entirely; `POST /api/auth/login`'s `emailOrPhone` accepts the 11-digit local phone; but `POST /api/auth/forgot-password` is email-only (it emails the OTP — omitting `email` 500s inside the mailer, and a phone value silently no-ops).
- **Rule:** Phone is the account identifier; wire the normalized local phone (`AppValidators.normalizeEgyptLocal`) into `emailOrPhone`. Omit the `email` key from register bodies when blank — don't send `""`. Password reset requires an email until the backend adds SMS OTP reset; keep register's optional email pitched as "for password recovery".
- **Where it applies:** `auth_remote_datasource.dart`, `LoginNotifier`, register step-2 validation, `forgot_password_screen.dart`.

### 2026-07-13 — build_runner --build-filter deletes everything else's outputs
- **What happened:** `dart run build_runner build --delete-conflicting-outputs --build-filter="lib/features/auth/..."` regenerated the auth files but **deleted every other tracked `.g.dart` in the repo**, breaking analyze until a full rebuild restored them.
- **Rule:** In this repo never combine `--build-filter` with `--delete-conflicting-outputs`; run the full `dart run build_runner build --delete-conflicting-outputs` (~16s). If generated files show as deleted in `git status`, rerun the full build rather than checking them out.
- **Where it applies:** All codegen (freezed/riverpod/json) regeneration.

### 2026-07-13 — Admin-dashboard prototype: responsive rules go last, size inline SVGs
- **What happened:** Making the dashboard prototype responsive, the mobile `.drawer` override silently lost because the media query sat mid-file before the base `.drawer` rule (media queries add no specificity — later equal-specificity rules win); separately, the new hamburger icon rendered 0×0 because topbar `<svg>`s had no CSS size (pre-existing: bell/help/search icons were invisible too).
- **Rule:** In `docs_business/admin-dashboard/src/styles.css`, all `@media` responsive overrides live in the final section of the file — never insert them mid-file. Inline SVG icons need an explicit CSS width/height on their container class. `xstore_admin_dashboard.html` is generated by inlining the three `src/` files — regenerate it after any src change, never hand-edit it.
- **Where it applies:** `docs_business/admin-dashboard/` (HTML prototype, not Flutter).

### 2026-07-15 — Medium_Phone_API_35 AVD has corrupted package-manager state
- **What happened:** `flutter run` on the Medium_Phone_API_35 emulator failed with "Error type 3: Activity class does not exist" despite a clean install; PM resolved zero activities for ALL sideloaded apps (xstore prod, dev, and unrelated apps), and reboot + reinstall didn't fix it. The APK itself was proven fine via `aapt dump badging`.
- **Rule:** When `am start` says an installed activity "does not exist", first check whether OTHER sideloaded apps resolve (`cmd package resolve-activity --brief -a android.intent.action.MAIN -c android.intent.category.LAUNCHER <pkg>`); if they all fail, the AVD's package-manager state is corrupted — switch AVDs (flutter_emulator works) instead of debugging the app. Note: wiping Medium_Phone_API_35 loses its signed-in Google account, which is needed for Google sign-in tests.
- **Where it applies:** All emulator runs/verification on this machine.

### 2026-07-15 — dart:developer log() is invisible in flutter run console
- **What happened:** A credential debug dump used `developer.log(...)`; nothing appeared in the `flutter run` output because dart:developer logs go to the VM/DevTools logging stream, not the run console — wasted a full rebuild cycle discovering it.
- **Rule:** For debug output that must be visible in `flutter run` / logcat, use `debugPrint` (one call per field keeps long tokens on single copy-pasteable logcat lines, <4KB each). Reserve `developer.log` for DevTools-structured logging.
- **Where it applies:** All debug/diagnostic printing in this repo.

### 2026-07-15 — Logout must clear local state even when provider sign-outs throw
- **What happened:** `AuthRepositoryImpl.logout()` awaited `signOutSocial()` (Firebase+Google+Facebook `Future.wait`) before the secure-storage deletes; the unconfigured Facebook SDK (placeholder app ID) made it throw, the deletes were skipped, and `restoreSession` re-hydrated the "logged-out" user — logout appeared to do nothing. The method's own comment promised local cleanup "always" runs, but only the remote call was guarded.
- **Rule:** In sign-out flows, every external/best-effort step (remote API, provider SDK sign-outs) gets its own try/catch; local session clearing runs unconditionally and is the source of truth. When a comment claims "always", verify the control flow actually guarantees it.
- **Where it applies:** `auth_repository_impl.dart` logout, forced 401 cleanup in `dio_provider`, any future account-deletion flow.

### 2026-07-17 — Adding a UserRole member touches four coupled places
- **What happened:** Adding `UserRole.courier` required coordinated changes: shell branches in `app_router.dart`, tab labels/icons in `xstore_bottom_nav.dart` (now length-driven, no hardcoded 5), and role-aware redirect fallbacks in `computeXStoreAuthRedirect` — `/home`/`/explore` only exist inside the consumer/vendor shells, so any redirect returning `AppRoutes.home` sends a courier to a "no route" error page.
- **Rule:** A new role = new shell branch list + matching nav tab list (kept in sync, both switch on `UserRole`) + a role-aware "home" target in every redirect fallback + restricting the role's tabs from other roles and vice versa. Grep `AppRoutes.home` in redirect logic whenever shell membership changes.
- **Where it applies:** `lib/core/router/`, `xstore_bottom_nav.dart`, any future role or shell-tab change.

### 2026-07-17 — Mock login matchers must key on phone digits
- **What happened:** A mock courier login matched identifiers containing "courier"/"driver", but the login UI is phone-first — the field validates an 11-digit Egyptian mobile and `LoginNotifier` sends `normalizeEgyptLocal(state.phone)` (digits only), so a keyword/email matcher was unreachable from the real screen.
- **Rule:** Any identifier-routed mock behavior in auth must match what the UI actually transmits: normalized phone digits (compare via `AppValidators.normalizeEgyptLocal` against the seed user's phone). Keep keyword forms only as test conveniences. Trace notifier → datasource before choosing a matcher key.
- **Where it applies:** `mock_users.dart` login matchers, any future identifier-based dev/demo shortcut in the auth flow.

### 2026-07-18 — Controllers created in show-dialog/sheet helper functions are never disposed
- **What happened:** A full memory audit found 10+ `TextEditingController`s created inside `show*Sheet`/dialog helper functions (checkout add-address sheet, order card reject/ship/review flows, order action buttons, cart quantity prompt, store-hours message sheet) with zero disposals — every open leaks the controllers.
- **Rule:** A controller created outside a `State` (in a `showDialog`/`showModalBottomSheet` helper) must be disposed after the sheet/dialog future resolves (`await show...; ctrl.dispose();`), or the sheet content must be a StatefulWidget owning its controllers. Grep helper functions for `TextEditingController(` during review.
- **Where it applies:** All `show*` helper functions and dialog builders in `presentation/widgets` and screens.

### 2026-07-18 — Static datasource caches survive logout
- **What happened:** `orders_remote_datasource.dart` keeps `static _consumerCache`/`_vendorCache` (and cart keeps `static _items`) that are never cleared on logout — a second account on the same device can read the previous user's orders from the warm cache.
- **Rule:** Datasource-level caches must be instance fields on autoDispose-scoped providers, or be explicitly cleared in the logout/forced-401 path alongside `resetProfileData`. No user-scoped data in `static` fields.
- **Where it applies:** `orders_remote_datasource.dart`, `cart_remote_datasource.dart`, any future datasource cache.

### 2026-07-18 — Family providers without autoDispose accumulate one instance per argument
- **What happened:** `vendorOrderDetailProvider` was a plain `StateNotifierProvider.family` — every order id ever opened kept its notifier and fetched order in memory for the rest of the session.
- **Rule:** `.family` providers for screen-scoped state must be `.autoDispose.family` (or codegen `@riverpod`, which defaults to autoDispose). A keepAlive family is a per-argument cache and must be a deliberate, commented decision.
- **Where it applies:** All `.family` providers, especially detail-screen notifiers keyed by entity id.

### 2026-07-17 — Sliver lists below the fold aren't built in widget tests
- **What happened:** A test expected 3 `DeliveryOrderCard`s but found 2 — the third lived in a lazily-built `SliverList` section below the 600px test viewport, so it was never constructed.
- **Rule:** In widget tests over lazy lists (SliverList/ListView.builder), assert only what fits the viewport, then `tester.scrollUntilVisible(...)` before asserting off-screen items — don't shrink the data to dodge it unless section ordering itself is under test.
- **Where it applies:** All widget tests over scrollable feature screens.

### 2026-07-18 — Count strings need ICU plurals from the start
- **What happened:** The delivery card's items summary rendered "1 items" — the arb key used a plain `{count} items` interpolation instead of a plural.
- **Rule:** Any l10n string containing a count uses ICU plural syntax in BOTH locales from the first draft — Arabic needs its own categories (`=1`, `=2`, `few`, `other`), not a copy of the English two-form plural.
- **Where it applies:** All new arb keys with numeric placeholders.

### 2026-07-18 — Web bootstrap dies before runApp; app is un-runnable on `flutter run -d web-server`
- **What happened:** A screenshot-capture session found the app renders a permanently blank canvas on web: `bootstrap()` awaits `Firebase.initializeApp(options: DefaultFirebaseOptions.forFlavor(...))` and `forFlavor` throws `UnsupportedError('Web Firebase options are not configured yet.')` under `kIsWeb`, so `runApp` is never reached. No visible error — just an empty `flt-scene-host`, while the DDC console logs a single DartError. Also: `google_sign_in_web` asserts at startup because no `google-signin-client_id` meta tag exists for web.
- **Rule:** Startup code must not `await` a call that is known to throw on a supported run target before `runApp`. Either register real web Firebase options, or branch on `kIsWeb` (skip AppCheck/FCM, supply web options) so mock-mode web runs. When debugging "blank canvas" on Flutter web, check `flt-scene-host` children and page errors first — a pre-runApp throw looks identical to a slow load.
- **Where it applies:** `lib/main.dart` bootstrap, `lib/core/firebase/firebase_options.dart`, any CI/tooling that runs the app with `-d web-server` (mock QA, screenshot jobs).

### 2026-07-18 — Vendor Incoming Orders list never renders: infinite-height constraint crash
- **What happened:** On `/vendor-orders` (mobile web viewport 390x844, MOCK=true) the header stats and "Needs Action — 8 orders" group render, but every order card fails layout with `BoxConstraints forces an infinite height` (box.dart:2251 assertions, then "Unexpected null value" and endless mouse_tracker asserts). The list area is permanently blank even though data is present.
- **Rule:** A widget inside a scrollable must not demand infinite height (e.g. unbounded `Column`/`Expanded` inside a sliver child, or `ListView` inside a `Column` without constraints). Reproduce vendor screens at a phone-sized viewport before merging; assertion spam from mouse_tracker after a layout throw is a symptom, not the cause — find the first `BoxConstraints`/`RenderBox` error above it.
- **Where it applies:** vendor incoming-orders screen widgets (order group list/cards), any sliver-based list screen.

### 2026-07-19 — Widget tests over mock-datasource screens leak a pending Timer + demo seeds
- **What happened:** Pumping the courier/consumer package screens in widget tests failed teardown with "A Timer is still pending even after the widget tree was disposed", and the datasource's demo seed data leaked into "empty"/scenario assertions. The delivery mock datasource returns via `MockConfig.simulate(...)`, which schedules a real delay Timer, and its constructor seeds demo requests.
- **Rule:** Widget tests that mount a screen backed by a mock/in-memory datasource must override the *repository* provider with a zero-latency, no-seed stub (see `test/helpers/stub_delivery_request_repository.dart`, mirroring `StubOrdersRepository`) — never let the real mock datasource run under `flutter_test`, or the simulated-latency Timer trips the pending-timer assert and seeds pollute scenarios. Provide a per-status entity factory for the stub so tests build exactly the state under test.
- **Where it applies:** All widget tests over delivery/orders/cart screens and any future feature whose datasource uses `MockConfig.simulate`.

### 2026-07-19 — Auth API "last version" contract shifted; several app flows were silently broken
- **What happened:** Live probing the xStoreEcommerce backend showed the app was out of sync: `POST /api/auth/login` now needs `phoneNumber` (app sent `emailOrPhone` → 400); `consumer/register` now REQUIRES a valid `email` (app omitted it when blank); `vendor/register` is multipart/form-data with a REQUIRED `profileImage` file (app sent JSON → 400 "The ProfileImage field is required."); `dateOfBirth` must be date-only `YYYY-MM-DD` (a full `toIso8601String()` timestamp is rejected). New endpoints exist: `send-login-otp`+`login-with-otp` (native passwordless login, OTP echoed in body, 404 for unknown phone), and role-split `google/{consumer|vendor}/login` taking a Google identity token (auto-creates the account). The old generic `/api/auth/social` is 404.
- **Rule:** Treat the login identifier key as `phoneNumber`; email is required in register; vendor register is multipart with a required image; wire birth dates as date-only. Google backend login takes the Google ID token (not a Firebase token) via a role-specific endpoint — pick the role BEFORE the call (the endpoint sets the created account's role), then trust get-profile for the authoritative role. Reconcile the app to the Postman collection by PROBING each endpoint, not by trusting either side blindly.
- **Where it applies:** `auth_remote_datasource.dart`, `api_endpoints.dart`, register validation/UI, `phone_auth_provider` (repointed to backend OTP), `social_auth_provider` + role screen.

### 2026-07-19 — The hosted backend mutates mid-session (schema drift during deploys)
- **What happened:** Endpoints that returned 200 early in the session (login, send-login-otp, consumer register) later 500'd with `"Invalid column name 'Lat'. Invalid column name 'Lng'."` — a redeploy added `Lat`/`Lng` user/store columns whose DB migration hadn't run, so every query touching the users table failed. Validation (400s) still worked, isolating it to the write/read DB path.
- **Rule:** When a previously-green live endpoint starts 500ing, re-probe a KNOWN-good call (login) before blaming your payload — a raw SQL/`Invalid column name` error in the body means the backend is mid-migration, not your bug. Don't conclude an app-side defect from 500s gathered during an active deploy; capture the exact error and note the backend state.
- **Where it applies:** All live-backend verification against the hosted integration server.

### 2026-07-19 — Interrupted subagents leave the tree referencing not-yet-created symbols
- **What happened:** Three parallel subagents building the package-delivery feature were killed mid-task by a session limit; they left screens/widgets calling ~40 l10n getters that didn't exist yet and a courier screen calling an unwritten `_packageList` builder — `flutter analyze` was red until the lead finished the arb keys and the missing method.
- **Rule:** After any multi-agent or interrupted change, the lead runs `flutter analyze` + full `flutter test` before declaring done — undefined-getter/undefined-method errors are the fingerprint of a half-applied edit (l10n keys, generated code, or a helper the caller was written against). Close them against the callers, then regenerate l10n and re-run.
- **Where it applies:** Any task delegated to subagents, especially split across presentation/data/l10n; the integration + verify pass is the lead's, not the agents'.

### 2026-07-21 — Avatar upload was a no-op stub: no file sent, dead route
- **What happened:** `ProfileRemoteDataSource.updateAvatar` did `_dio.post('/users/{id}/avatar')` with no body against a route its own comment admitted didn't exist, then returned a fake `MockImages.avatar(99)` URL — so changing the profile image always errored, and the provider aborts the whole save on that failure (`saveProfile` returns early on avatar failure). Fixed to send the picked file as `FormData` multipart with field `profileImage` (the only confirmed image field in this backend — vendor register), route centralized in `ApiEndpoints.uploadAvatar`, response parsed tolerantly, errors via `mapDioException`.
- **Rule:** An image-upload datasource method MUST attach the file as a `MultipartFile` in `FormData` (field `profileImage` for this backend) and map errors via `mapDioException` — never ship a stub POST that ignores `filePath` or hits a route commented as non-existent. Before claiming a "missing auth header" defect, check `dio_provider`'s interceptor: `X-Auth-Token` (and the Basic license key) are injected centrally on every request, so per-call `ApiAuthHeaders.authenticated()` is intent-only, not the auth mechanism.
- **Where it applies:** `profile_remote_datasource.dart` `updateAvatar`, any future file-upload datasource (listing images), and any auth-defect diagnosis.

### 2026-07-23 — Network image loads bypass Dio auth
- **What happened:** Backend-hosted photos 401'd in the UI because `CachedNetworkImage`/`NetworkImage`/`Image.network` fetch over plain HTTP clients — they never inherit `dio_provider`'s default `Authorization: Basic …` header.
- **Rule:** All remote image display goes through `AppCachedNetworkImage` / `AppNetworkImage.cached` / `AppNetworkImage.network` (or `AppNetworkImageHeaders.httpHeaders` explicitly). `AppImageCacheManager` injects the same Basic license key on cache fetches. Do not call raw `CachedNetworkImage`/`Image.network` for backend URLs.
- **Where it applies:** `image_cache_manager.dart`, `app_cached_network_image.dart`, any new avatar/product/banner thumbnail.

### 2026-07-23 — get-profile nests store under `store`, not on `user`
- **What happened:** Live get-profile returns user identity and store details in separate objects; vendor UI gated on `role == vendor` missed vendors when role wasn't echoed on the user object, and store fields on the nested `store` key were never merged into `UserModel`.
- **Rule:** Parse profile responses with `parseProfileResponse` / `userModelFromProfileResponse`: merge `store` into flat user keys (`nameEn`→`storeNameEn`, `lat`/`lng`, address fields, `storeId`), surface `isEmailVerificationRequired`/`isPhoneVerificationRequired` on `ProfileEntity`, and treat vendor profile UI as `user.hasStore` (`storeId != null`) — not role alone.
- **Where it applies:** `user_model.dart`, `profile_remote_datasource.dart`, `auth_remote_datasource.dart`, `profile_screen.dart`.

### 2026-07-25 — Coalesce get-profile refreshes; 429 needs cooldown not retry storm
- **What happened:** Entering profile/edit profile fired overlapping `refreshProfileData()` calls (prefetch + tab + edit-screen mount), hammering `/api/auth/get-profile` until the backend returned HTTP 429.
- **Rule:** Single in-flight refresh Future shared by all callers; skip automatic re-fetch within 30s when profile is already loaded; after 429 set a 1-minute cooldown and map to `rateLimitErrorCode` — never mount-time refresh on edit profile (prefetch/tab already load data). Pull-to-refresh and post-save use `force: true`.
- **Where it applies:** `profile_provider.dart`, `profile_screen.dart`, `edit_profile_screen.dart`, `dio_error_mapper.dart`.
- **What happened:** Live probe: JSON PUT bodies are ignored (200 but no changes); multipart `storeDescriptionEn` persists but `birthDate` is returned unchanged even when sent. Dio `LoggingInterceptor` printed `FormData` as an opaque instance so profile save debugging was blind.
- **Rule:** Never send update-profile as JSON — always `FormData`. Expand `FormData` to `{field: value, file: "<file:name>"}` in debug logs. Log wire vs raw response vs parsed model for `birthDate` and `storeDescriptionEn`; flag when sent birthDate ≠ returned (backend gap). JSON PUT is a silent no-op on this backend.
- **Where it applies:** `profile_remote_datasource.dart`, `logging_interceptor.dart`, live-backend verification of profile writes.
- **What happened:** Updated birthdate saved to the API but the edit-profile field stayed empty/stale — `DateTime.tryParse` on ISO timestamps could shift the calendar day, `==` compared pickers vs wire timestamps as unequal, and `_synced` blocked controller refresh after `refreshProfileData()` in `initState`.
- **Rule:** Parse `birthDate`/`dateOfBirth` as a literal `YYYY-MM-DD` calendar date (`dateOnly: true` in `UserModel.fromJson`); compare DOB with year/month/day only; normalize date-picker values to date-only; re-sync edit-profile controllers when `isLoading` finishes and `!hasChanges`.
- **Where it applies:** `user_model.dart`, `profile_provider.dart`, `edit_profile_screen.dart`.
- **What happened:** After saving an updated store description, edit profile and vendor store screens showed stale/empty text even though the API saved and returned `store.descriptionEn`/`descriptionAr` on the nested store object.
- **Rule:** Mirror the store-name bilingual pattern for descriptions: merge `descriptionEn`/`descriptionAr` → `storeDescriptionEn`/`storeDescriptionAr` in `mergeStoreJsonIntoUser`; in `UserModel.fromJson` populate legacy `storeDescription` from En→Ar→legacy; in `applyFromProfile` and edit-equality checks read `storeDescriptionEn`/`storeDescriptionAr`, not legacy `storeDescription` alone.
- **Where it applies:** `user_model.dart`, `profile_state.dart`, `profile_provider.dart`, any UI displaying vendor store description.
- **What happened:** `google-services.json` only listed the debug keystore SHA-1 (`5e451d…`); release APKs signed with `android/app/release.keystore` (or the CI/root keystore) got `DEVELOPER_ERROR` / no Google ID token. Debug `flutter run` uses the debug keystore so social login and Google-backed register appeared fine.
- **Rule:** Every release signing key (local `key.properties` keystore AND the CI `KEYSTORE_BASE64` secret — they may differ) must have SHA-1 **and** SHA-256 registered in Firebase for both `com.xstore.app` and `com.xstore.app.dev`, then re-download/commit `google-services.json` and rebuild the release APK. Run `task signing:report` or `./gradlew :app:signingReport` to list fingerprints; never assume the debug SHA covers release.
- **Where it applies:** `android/app/google-services.json`, `android/app/src/dev/google-services.json`, Firebase console / `firebase apps:android:sha:create`, CI release workflow.

### 2026-07-23 — Empty user id breaks `/seller/:sellerId` navigation
- **What happened:** Live get-profile omits `user.id`; tapping "Manage store" pushed `/seller/` (empty segment) → GoRouter "no routes for location `/seller`".
- **Rule:** Never push `AppRoutes.sellerProfile` without a non-empty id — use `AppRoutes.sellerPath(id)` and resolve id from profile user, auth session, then JWT (`userIdFromJwt`) on login/restore. Disable the CTA when id is still unknown.
- **Where it applies:** `profile_screen.dart`, `jwt_payload.dart`, `auth_repository_impl.dart` `_resolveFullUser`/`restoreSession`, `userModelFromProfileResponse`.

### 2026-07-26 — Cache reference-data providers via keepAlive-on-success, not keepAlive:true
- **What happened:** Registration reference lists (`allCities`/`allGovernments`/`allStoreCategories`) were plain autoDispose `@riverpod`, so leaving and re-opening the register flow re-hit the network for all three every time.
- **Rule:** For static reference-data list providers, keep them `@riverpod` (autoDispose) and call `ref.keepAlive()` inside the SUCCESS branch of the fold — caches values for the app session while leaving failures unpinned so re-entry retries. Prefer this over `@Riverpod(keepAlive: true)`, which would also cache an AsyncError permanently (dropdowns break with no retry).
- **Where it applies:** `city_dependencies.dart`, `government_dependencies.dart`, `store_category_dependencies.dart`, any future lookup/reference list provider.

### 2026-07-26 — cities/governments hierarchy (updated 2026-08-23)
- **What happened (2026-07-26):** An earlier live probe showed an inverted hierarchy (cities as parents, governments as districts via `Government.cityId`), and the cascade was built around that.
- **What happened (2026-08-11):** Buyer register's government popup showed no usable cascade — live `/api/governorates` now returns real governorates (`{id,nameEn,nameAr}` only) and `/api/cities` returns cities with `governorateId` pointing up. Filtering governments by `cityId` always yielded an empty city sheet.
- **What happened (2026-08-23):** Government sheet showed the generic error. Live `/api/governorates` and `/api/cities` now return a **bare JSON array** (27 governorates, 240 cities; `pageSize` ignored). Datasources still did `get<Map<String, dynamic>>` expecting `{items,totalCount}`, so Dio threw a type error and the sheet rendered `genericError`.
- **Rule:** Location cascade matches Egypt's real hierarchy and the current live API: popup 1 = `allGovernments` → writes `governmentId`/`storeGovernmentId` (labeled Governorate); popup 2 = `allCities` filtered by `governorateId == chosen` → writes `cityId`/`storeCityId` (labeled City). Parse cities with `governorateId` (fallback `governmentId`). GET reference lists as `dynamic` and unwrap a bare array **or** `{items|data|results}` — never assume the pagination envelope. Do not trust old inverted-hierarchy comments or an old envelope lesson — re-probe both list endpoints when location pickers break or error. Shared picker: `location_cascade_field.dart`. Changing a governorate resets the dependent city via `updateStoreLocation`.
- **Where it applies:** `location_cascade_field.dart`, `government_remote_datasource.dart`, `city_remote_datasource.dart`, `json_list_unwrap.dart`, register + edit profile location fields.

### 2026-07-26 — One shared user location; consumer register omits location by default
- **What happened:** The government→city cascade was first added only to the vendor store step + profile store section, but the personal "Location / City" field (register step 2, all roles) was free-text and — critically — NOT sent on the wire at all (consumer register only posts name/email/phone/password/DOB; the personal `location` string only fed the dead legacy `register` path). Backend has ONE structured `cityId`/`governmentId` pair, so two cascades would conflict.
- **Rule:** Treat city+government as ONE user-level location: the step-2 cascade (all roles) drives `storeCityId`/`storeGovernmentId`, the vendor store step does NOT ask again, and the requirement lives in step-2 validation (`storeLocation` key), not step 4. Consumer register must explicitly add `cityId`/`governmentId` to `ConsumerRegisterParams` + the POST body (extra unbound fields are ignored by the backend, so it's safe — flag as needs-confirmation). In edit profile the single cascade lives in the general all-roles area, not inside `if (isVendor)`. Before wiring a UI field, trace whether its value actually reaches the wire — several register fields are collected but never sent.
- **Where it applies:** `register_screen.dart` (_StepPersonal cascade, _StepStore no location), `auth_provider.dart` validation + consumer submit, `consumer_register_params.dart`, `auth_remote_datasource.dart` registerConsumer body, `edit_profile_screen.dart`.

### 2026-07-26 — Reference-data pageSize max is 100
- **What happened:** `allGovernments` / `allCities` / `allStoreCategories` requested `pageSize: 200`; live API returned 400 — `'Page Size' must be between 1 and 100`.
- **Rule:** Never pass `pageSize > 100` to paginated reference-data endpoints. For "fetch all" dropdown providers use 100 (the API max); if a table can exceed 100 rows, loop pages with `PaginatedResult.hasNextPage` instead of raising page size.
- **Where it applies:** `government_dependencies.dart`, `city_dependencies.dart`, `store_category_dependencies.dart`, any future paginated list fetch.

### 2026-07-26 — Birth date must not be after today
- **What happened:** User asked to enforce birth dates app-wide; picker/validators initially used "strictly before today" (yesterday max) but the intended rule is **no dates after today** (today and past are OK).
- **Rule:** Centralize in `Validators.isBirthDateAfterToday` / `isSelectableBirthDate` / `latestBirthDate()` (= today). Picker uses `selectableDayPredicate`; registration validates on pick + submit (with optional min-age), profile on save. Future dates always rejected.
- **Where it applies:** `validators.dart`, `birth_date_picker.dart`, `register_screen.dart`, `edit_profile_screen.dart`, `auth_provider.dart`, `profile_provider.dart`.

### 2026-07-26 — Birth-date picker needs selectableDayPredicate
- **What happened:** `showDatePicker` with `lastDate: yesterday` alone still let users tap today on some platforms; registration felt unenforced. Later clarified: block dates **after** today only (today is allowed).
- **Rule:** Shared `pickBirthDate` sets `lastDate: todayCalendarDate()`, `selectableDayPredicate: isSelectableBirthDate` (today or earlier). Reject future dates in validators via `isBirthDateAfterToday` — not "strictly before today".
- **Where it applies:** `birth_date_picker.dart`, `validators.dart`, register + edit-profile DOB fields.

### 2026-08-02 — Postman collection superseded the "flat JSON listing write" contract
- **What happened:** A fresh `xStoreEcommerce API.postman_collection.json` handed over by the user showed POST/PUT `/api/listings` as **multipart/form-data**, not the flat-JSON-with-int-condition body an earlier lesson (2026-07-11) had "CONFIRMED" via live probe. Attributes bind as an indexed list (`Attributes[0].Key`/`Attributes[0].Value`, ASP.NET's default `List<T>` form binder — repeat `Attributes[i].Key`/`Attributes[i].Value` per entry), and images attach inline as repeated `imageFiles` parts on the same request — there is no separate pre-upload-then-URL step for listings (unlike avatar/profile images). The old contract had never actually been re-verified after the backend evolved; images were consequently a total no-op (`imageUrls: const []` always sent, `state.photoPaths` never left the device).
- **Rule:** A "CONFIRMED via live probe" lesson is a snapshot, not a permanent fact — the hosted backend has drifted before (see the 2026-07-19 auth contract rewrite) and can again. When the user hands over a fresh Postman collection, treat it as the current source of truth over an old lesson, but still live-probe if possible before trusting it blindly (this session couldn't — the hosted backend was down, HTTP 500.30 "app failed to start", confirmed via `/api/categories` on a clean GET, unrelated to any payload). Fields not shown in a collection's example body (here: `condition`, `subcategoryId`, `status`) are safe to keep sending defensively — extra unbound multipart keys are ignored by ASP.NET model binding, and dropping them would silently break condition filtering / the pause-resume status mutation if the backend does bind them.
- **Where it applies:** `listing_remote_datasource.dart` (`_listingFormData`), any future reconciliation against a newly-supplied Postman collection for any feature.

### 2026-08-02 — Local photoPaths need an existence guard before multipart upload
- **What happened:** Wiring `state.photoPaths` (image_picker local file paths, persisted into a "Save Draft" `SharedPreferences` blob across app sessions) into `MultipartFile.fromFile` for the first time surfaced a new failure mode: the OS can evict image_picker's cache between when a draft was saved and when it's resubmitted, so a stale path throws a raw file-system exception and hard-fails the entire listing submit over one missing photo.
- **Rule:** Any datasource attaching locally-cached file paths as multipart uploads must check `File(path).existsSync()` per path and skip (not throw on) a missing one — especially when those paths can be restored from persisted draft state rather than freshly picked in the same session.
- **Where it applies:** `listing_remote_datasource.dart` `_listingFormData`, any future feature reading persisted local file paths (draft state, offline queues) for upload.

### 2026-08-02 — Business caps ("max N photos") need enforcement at every layer that can reintroduce N, not just the add-path
- **What happened:** The listing photo cap (5) was already enforced correctly at the one place a user adds a photo (`addPhotoPath`) and mirrored in the UI (`PhotoUploadSection` hides the add-tile at 5). But `_stateFromJson` (draft restore from `SharedPreferences`) copied `photoPaths` straight through with no clamp — a draft saved by an older build, or a hand-edited blob, could carry more than 5 and they'd all reach the network once image upload actually started working.
- **Rule:** When a business rule is "never more than N" and the field can be populated from more than one entry point (fresh user action AND persisted/restored state AND the eventual network write), clamp at each entry point independently — don't rely on the UI-facing guard alone. For listings: `addPhotoPath` (add-time), `_stateFromJson` (draft restore, via `.take(_maxPhotos)`), and `_listingFormData` (`imagePaths.take(_maxImagesPerListing)`, the actual network boundary — the one guarantee that holds no matter what upstream state looked like).
- **Where it applies:** `listing_form_notifier.dart`, `listing_remote_datasource.dart`; any future numeric cap on user-generated content that can flow through drafts/restore paths.

### 2026-08-02 — flutter_image_compress: compress client-side before network attach, fail open
- **What happened:** Listing photos were being sent full-resolution/unswed straight from `image_picker`'s cache. Added `flutter_image_compress: ^2.5.1` (`FlutterImageCompress.compressAndGetFile(sourcePath, targetPath, quality: 80, minWidth: 1600, minHeight: 1600)` — returns `Future<XFile?>`, re-exports `XFile` from `cross_file` so no extra import is needed) at pick-time in `pickFromCamera`/`pickFromGallery`, writing to `'$sourcePath-compressed.jpg'` (same directory as the source — avoids adding `path_provider` as a new direct dependency just for a temp dir, and sidesteps the package's own same-path guard that throws if source/target resolve equal).
- **Rule:** Wrap the compress call in try/catch and fall back to the original uncompressed path on any failure — a codec/format quirk on one device must never block adding a photo. Compress once at pick-time, not again at submit-time, so drafts/attribute edits don't recompress repeatedly.
- **Where it applies:** `listing_form_notifier.dart` `_compressPhoto`; any future feature attaching picker-sourced images to a multipart upload.

### 2026-08-02 — Resubmit-after-rejection: not-yet-confirmed wire fields, and don't double-toast errors
- **What happened:** Built the `PUT /api/listings/{id}/resubmit` flow (plain JSON `{"newPrice": ...}`, unlike create/update's multipart — confirmed from the Postman collection's own body for that one request). Two things surfaced: (1) there's no example response anywhere in the collection for a rejected listing, so the wire key for the admin's rejection reason is genuinely unconfirmed — parsed tolerantly via `rejectionReason`/`rejectReason`/`adminRejectionReason`/`reason` with null (not `''`) fallback, same tolerant-parsing pattern as other unconfirmed enums in this repo. (2) First draft of the caller (`my_listings_screen._openResubmitSheet`) manually toasted `AppSnackbar.error` on failure — but `MyListingsNotifier` mutations already flow failures into `state.error`, and the screen already has a `ref.listen` on that field firing a global error toast (the exact mechanism pause/resume already relies on). The manual call would have double-toasted. Caught in self-review before it shipped.
- **Rule:** Before adding an explicit error toast at a mutation call site, check whether the owning notifier's failure path already writes to a `state.error`/similar field with an existing screen-level `ref.listen` — if so, only toast the success case locally and let the existing listener own errors. When a Postman collection request body differs in transport (JSON vs multipart) between sibling endpoints on the same resource, trust the specific request's own body over an assumed "all writes on this resource are multipart" pattern.
- **Where it applies:** `my_listings_notifier.dart`, `my_listings_screen.dart`, any future per-listing mutation with its own confirmation sheet.

### 2026-08-03 — A form sheet must not auto-close on failure the same way a menu sheet does
- **What happened:** Team-lead review of `ResubmitListingSheet` found `_submit()` calls `Navigator.of(context).pop(ok)` unconditionally after `onSubmit` resolves, even when `ok == false`. The caller's comment justified this as "same pattern pause/resume relies on" — but pause/resume close a menu-style options sheet with no user-entered data at stake, then let the screen's `ref.listen` show the error toast. Resubmit's sheet holds a typed price and displayed rejection reason; closing it on failure forces the vendor to reopen it and retype the price to retry.
- **Rule:** A confirmation/options sheet (no form state) may close unconditionally and let a screen-level `ref.listen` own error display. A sheet holding user-entered form state must stay open on failure — show the error inline (or via a scoped listener) and only pop on success — even if that means duplicating part of the "toast on error" logic locally instead of reusing the fire-and-forget menu-sheet pattern.
- **Where it applies:** `resubmit_listing_sheet.dart`; any future bottom sheet that combines user input with an async submit action.
- **Fixed 2026-08-03:** `_submit()` now only pops on success; on failure it sets `_submitting = false` and shows `context.l10n.errorGeneric` inline via the existing `_error`/`errorText` field, keeping the typed price and visible rejection reason intact for a retry.

### 2026-08-03 — Multipart PUT that drops imageFiles may not mean "leave images unchanged"
- **What happened:** Review of the pause/resume listing-status mutation (`my_listings_notifier.dart` `_mutateListingStatus` → `updateListingUseCaseProvider`) found it now sends `imagePaths: const []` into the multipart `PUT /api/listings` introduced by the 2026-08-02 Postman-contract rewrite. Before that rewrite, the same call resent the listing's existing `imageUrls` in a flat JSON body — a real behavior change, not just a rename. The code comment asserts "the backend keeps existing images when imageFiles is omitted," but this was never live-probed (the hosted backend was down all session) and the Postman collection's own Update Listing example still includes an (empty) `imageFiles` form field, which doesn't resolve whether omission means "no change" or "clear."
- **Rule:** When a multipart PUT used for a *partial* mutation (status-only, price-only, etc.) shares an endpoint with a *full* create/update that carries images, do not assume omitting the image part is a no-op — call out the assumption in the PR/review explicitly and get a live-probe or backend confirmation before shipping a flow that runs on every pause/resume. Never quietly reuse a full-resource write endpoint for a partial mutation without checking what fields it treats as authoritative-and-replacing vs optional-and-preserving.
- **Where it applies:** `my_listings_notifier.dart` `_mutateListingStatus`, any future partial mutation routed through a shared multipart create/update endpoint.
- **Partially fixed 2026-08-03:** `pauseListing` now goes through the dedicated, confirmed `PUT /api/listings/{id}/deactivate` (`ApiEndpoints.apiListingDeactivate`, `ListingRemoteDataSource.deactivateListing`) — a bodyless status-only PUT that never touches `imageFiles`, so pause no longer carries this risk. `resumeListing` still has no confirmed dedicated "activate" endpoint in the collection and was deliberately NOT given a guessed one; it still runs through the generic multipart update with `imagePaths: const []`. Clarified 2026-08-03: this IS already the strongest client-side mitigation possible — `_listingFormData` only adds an `imageFiles` part per path actually present, so an empty list means the field is fully absent from the multipart body, not present-but-empty. The residual risk is purely how the backend interprets an absent `imageFiles` part on a shared write endpoint, which no client-side change can resolve — only a live probe or backend confirmation can close this out. `_mutateListingStatus` was refactored into `_applyOptimisticStatusMutation` (shared optimistic-update/rollback scaffolding, network call injected per caller) so pause/resume no longer share a single network call.

### 2026-08-03 — Endpoint audit against a fresh Postman collection found four real drifts
- **What happened:** Comparing `lib/core/network/api_endpoints.dart` + datasources against the live `xStoreEcommerce API.postman_collection.json` (handed to the user, not committed in-repo — found in `~/Downloads/`) surfaced: (1) `ApiEndpoints.governments` calls `/api/governments`, but the confirmed collection uses `/api/governorates` — a plain rename the app never picked up. (2) Avatar upload (`ApiEndpoints.uploadAvatar` = `/api/auth/avatar`, field `profileImage`) was always marked "ASSUMPTION — not confirmed" in its own code comment; the collection has a real, generic `POST /api/uploads/{entityType}` (entityType=avatar/listing/banner) with field `file` that the app never adopted. (3) Home screen (`home_remote_datasource.dart`) serves **hardcoded static placeholder banners** (picsum.photos URLs) with a comment claiming "no banners endpoint exists" — the collection has a full `/api/banners` CRUD folder, plus a dedicated `GET /api/home` aggregate endpoint the app never calls (it instead derives hot-deals/categories by hand from `/api/listings` + `/api/categories`). (4) `deleteListing` calls `DELETE /api/listings/{id}`, but the collection's Listings folder has no DELETE endpoint at all — only `PUT .../cancel` and `PUT .../deactivate` — so the live "Delete listing" menu action may be hitting a route that doesn't exist.
- **Rule:** When a fresh Postman collection is handed over, don't just reconcile the endpoints touched by the current task — grep `api_endpoints.dart` end-to-end against the collection's full folder list once, since drift accumulates silently in routes nobody is actively working on (governorates, avatar upload, banners had all drifted independently of each other, in different sessions). Flag any app route whose own code comment says "assumption"/"not confirmed" as a standing audit item, not just an FYI.
- **Where it applies:** `api_endpoints.dart`, `government_remote_datasource.dart`, `profile_remote_datasource.dart` `updateAvatar`, `home_remote_datasource.dart`, `listing_remote_datasource.dart` `deleteListing`.
- **Fixed 2026-08-03 (1, 2, 3 of 4):** (1) `ApiEndpoints.governments` now points at `/api/governorates`. (2) Avatar upload now posts to `ApiEndpoints.apiUpload('avatar')` (`/api/uploads/avatar`) with multipart field `file`, replacing the unconfirmed `/api/auth/avatar` + `profileImage` guess; contract-tested in `test/features/profile/profile_update_payload_test.dart`. (3) `home_remote_datasource.dart` `fetchBanners()` now calls the confirmed `GET /api/banners` with tolerant field parsing (`nameEn`/`titleEn`/`name`/`title`, `imageUrl`/`image`/`bannerImageUrl`), falling back to the static placeholder set only on error or an empty/malformed response — the dedicated `GET /api/home` aggregate endpoint was deliberately left unused since the collection has no example response for it and guessing its shape would risk silently breaking hot-deals/categories, which already work by deriving from `/api/listings` + `/api/categories`.
- **Fixed 2026-08-03 (4 of 4, on explicit user instruction):** `deleteListing` now PUTs to `ApiEndpoints.apiListingCancel` (`/api/listings/{id}/cancel`) instead of the nonexistent DELETE. Chose `/cancel` over `/deactivate` because `/deactivate` is already pause's endpoint — reusing it for delete would make a "deleted" listing reappear as merely paused-and-resumable on the backend. Since `/cancel` is a soft status change (not a hard row delete) and the "cancelled" status code isn't confirmed (would parse to an unmapped default via `_listingStatusFromDto`), `MyListingsNotifier.deleteListing` no longer calls `fetchListings()` after a successful delete — trusts the optimistic local removal instead of risking the item reappearing mislabeled on refetch. Contract-tested in `test/listing_resubmit_test.dart`. Residual gap: the confirmation dialog's copy ("will be removed permanently") slightly overpromises versus the backend's actual soft-cancel semantics — a copy change, not a code bug, and out of scope here.

### 2026-08-03 — Offline fallback for an id-only mutation only works if the entity is already cached
- **What happened:** `resubmitListing`/`deactivateListing` were missing the `_isOffline` fallback that `createListing`/`updateListing`/`deleteListing` all have. Unlike those, resubmit/deactivate only receive an `id` (+ `newPrice` for resubmit) — not the full listing fields — so there's no way to fabricate a complete `ListingModel` offline scaffold from scratch the way `createListing` does.
- **Rule:** For an id-only mutation, an offline fallback can only be meaningful by mutating an existing entry in the local cache (`_localMine`) if one is present; if it isn't cached, rethrow via `mapDioException` rather than fabricating incomplete data. Don't skip the fallback branch entirely just because a full scaffold isn't possible — a partial, cache-hit-only fallback is still strictly better than none, and keeps the method consistent with its siblings' error-handling shape.
- **Where it applies:** `listing_remote_datasource.dart` `resubmitListing`/`deactivateListing`; any future id-only mutation added alongside full create/update methods that already have offline scaffolding.

### 2026-08-02 — Some sessions have no Flutter/Dart SDK; hand-mirror freezed regen
- **What happened:** Extending package delivery requests to vendors required renaming `DeliveryRequestEntity` fields (`consumerId/consumerName/consumerPhone` → `requesterId/requesterName/requesterPhone`), but this session's environment has no `flutter`/`dart` binary at all — `dart run build_runner build` cannot run to regenerate `delivery_request.freezed.dart`. Recurred the same day on an unrelated go_router `errorBuilder` change: no SDK anywhere on disk (only `windows/flutter`, `linux/flutter` etc. build-plugin folders, not an SDK).
- **Rule:** Check for a working `dart`/`flutter` binary (`which flutter dart`) before planning any codegen-touching change or promising to run `flutter analyze`/`flutter test`. If absent, hand-edit generated `.freezed.dart`/`.g.dart` files with a scoped, mechanical rename (e.g. `sed` over just the renamed identifiers, verified with a follow-up grep for leftovers) rather than skipping the regen — freezed output is a deterministic mirror of the field list, so a careful identifier-for-identifier substitution is safe when the rename doesn't add/remove/retype fields. For non-codegen changes, review the diff manually against existing conventions instead. State plainly in the final report that `flutter analyze`/`flutter test` could not be run and verification was manual — never claim a green build you didn't observe. Re-check per session; a different container may have the SDK.
- **Where it applies:** Any field rename or freezed/riverpod-codegen touching change; any Dart change in a session without the SDK.

### 2026-08-02 — Scope identifier renames to the entity that's actually changing meaning
- **What happened:** Same task: `consumerId`/`consumerName`/`consumerPhone` also appear on `OrderEntity` (marketplace orders, always a real consumer buyer) and in unrelated cart/wishlist code. A repo-wide rename would have been wrong — only `DeliveryRequestEntity`'s fields needed to become role-neutral (`requesterId`/…), since a vendor can now be the sender.
- **Rule:** Before a mechanical rename (sed/replace_all), grep the identifier repo-wide first and manually bucket every hit by which entity/feature it belongs to. Only touch the files belonging to the entity whose semantics actually changed; same-named fields on siblings that are still correct must be left alone.
- **Where it applies:** Any field/identifier rename that shares a name across multiple entities (`consumerId`, `vendorId`, `courierId`, `status`, etc. all recur across `orders`/`delivery`/`cart`).

### 2026-08-03 — FCM UX: bootstrap setup, iOS foreground APS, Android local notifications
- **What happened:** Push was register-only; Console tests looked broken on iOS foreground and TestFlight lacked production APS.
- **Rule:** Call `configureFirebaseCloudMessaging()` once in `bootstrap()` (background handler registration + iOS `setForegroundNotificationPresentationOptions` + Android local-notification channel). Wire `fcmPushHandlingProvider` from the app root for `onMessage` / tap / cold-start routing via `routeFromRemoteMessage` → `goRouterProvider.go`. Use `RunnerRelease.entitlements` (`production`) for Release/Profile; keep `development` on Debug. Android foreground needs `flutter_local_notifications` — iOS foreground uses presentation options, not duplicate local notifications.
- **Where it applies:** `lib/core/firebase/fcm_*.dart`, `fcm_push_handling_provider.dart`, `main.dart`, iOS entitlements, Android default FCM channel meta-data.

### 2026-08-03 — Team-lead audit of the FCM push handling change (above): real gaps found
- **What happened:** Auditing the "FCM UX" change found: (1) both the Android manifest's `default_notification_icon` and `AndroidInitializationSettings('@mipmap/ic_launcher')` in `fcm_local_notifications.dart` point at the full-color launcher icon (`ic_launcher.png` — purple/white/orange) — Android renders notification icons from the alpha channel only since Lollipop, so this will show as a plain white blob, not the logo, no dedicated monochrome asset exists in `res/`. (2) `initFcmLocalNotifications()` wires `onDidReceiveNotificationResponse` for taps while the app process is alive, but never calls `getNotificationAppLaunchDetails()` — if a foreground-shown Android local notification is tapped *after* the app was backgrounded/killed (cold launch from the tray), the tap is silently dropped and no route fires. (3) `_isSafeInAppRoute` is duplicated verbatim in `fcm_message_route.dart` and `fcm_push_navigation.dart` — two call sites now exist, past the project's own "two usages, then abstract" bar. (4) `flutter test` (full suite, unrelated to this change) surfaced two pre-existing failures worth knowing about separately: `logging_interceptor_test.dart` shows `X-Auth-Token` is not redacted in debug request logs (only `Authorization`/body fields are), and `auth_redirect_and_providers_test.dart`'s "register failure after valid wizard" case fails — both predate and are unrelated to the FCM work (not in `lib/core/firebase/` or `notifications/`), traced to the separately in-progress `server_error_provider`/`dio_provider` changes already in the working tree.
- **Rule:** Any Android notification-icon resource (manifest `default_notification_icon` and any `flutter_local_notifications` `AndroidInitializationSettings`) must be a dedicated white-silhouette/alpha-only drawable — never reuse `@mipmap/ic_launcher`. Any `flutter_local_notifications` init that wires a runtime tap callback must also call `getNotificationAppLaunchDetails()` at startup and route from `details.notificationResponse` when `didNotificationLaunchApp` is true, or cold-launch taps are lost. When a small pure helper (route-safety check, etc.) gets a second call site, extract it instead of copy-pasting — matches the repo's own abstraction-timing rule.
- **Where it applies:** `android/app/src/main/AndroidManifest.xml`, `lib/core/firebase/fcm_local_notifications.dart`, `lib/core/firebase/fcm_message_route.dart` + `fcm_push_navigation.dart`; `logging_interceptor.dart` and the register-failure test are flagged separately for whoever owns `server_error_provider`.
- **Fixed 2026-08-03:** Generated a real alpha-channel notification icon (`ic_stat_notify.png`, white "X" silhouette on transparent, per-density in `res/drawable-{m,h,xh,xxh,xxxh}dpi/`) and pointed both the manifest's `default_notification_icon` and `AndroidInitializationSettings` at `@drawable/ic_stat_notify` instead of the color launcher icon. Added `getNotificationAppLaunchDetails()` in `initFcmLocalNotifications()`, staged into a module-level `_pendingLaunchRoute` consumed by `fcmPushHandlingProvider` via a new `_handlePendingLocalNotificationLaunch`, alongside the existing FCM `getInitialMessage()` cold-start path — same `deferUntilAuthenticated: true` staging. Extracted the duplicated route-safety guard into one `isSafeInAppRoute` (in `fcm_message_route.dart`, imported by `fcm_push_navigation.dart`) now that it had two call sites. Also moved the OS notification-permission prompt (`requestFcmNotificationPermission()`) to fire unawaited at `bootstrap()` for every user (logged in or not) instead of only after login/backend-token-sync — repeated calls are safe since the OS only shows the dialog once and returns the cached decision after. No environment to generate a PNG with (`ImageMagick`/`PIL` unavailable) — wrote a minimal pure-Python PNG encoder to draw the alpha-only glyph directly; verified the output by decoding the alpha channel back to ASCII art before wiring it in, not just by file size.

### 2026-08-03 — Never pass a widget's WidgetRef into a fire-and-forget background task
- **What happened:** Building app-entry location auto-detect (splash → silent profile location fill), the first draft wrote `Future<void> autoDetectAndFillLocationIfMissing(WidgetRef ref)` as a top-level function and called it as `unawaited(autoDetectAndFillLocationIfMissing(ref))` from `SplashScreen`'s `_bootstrap()`, right before `context.go(AppRoutes.home)`. `flutter_riverpod`'s `ConsumerStatefulElement.read()`/`.watch()` call `_assertNotDisposed()`, which throws `StateError('Cannot use "ref" after the widget was disposed.')` once `context.mounted` is false (verified in `flutter_riverpod-2.5.1/lib/src/consumer.dart:548-550,618-621`) — and since location detection can take up to 15s (the `LocationService` timeout) while splash navigates away and gets disposed almost immediately, the later `ref.read(...)` calls inside the unawaited function were near-certain to throw. Caught in self-review before it shipped, not by the hook or the user.
- **Rule:** A `WidgetRef` (from `ConsumerState`/`ConsumerWidget`) must never be captured into a `Future` that is expected to keep running (fire-and-forget, `unawaited`) past that widget's own lifetime — it throws the instant the widget disposes. For background work triggered from a widget but meant to outlive it, do the one-shot `ref.read(someKeepAliveProvider.notifier)` synchronously (safe, widget still mounted), then call an async method ON that notifier that uses its own internal `ref` (tied to the keepAlive provider's container-length lifetime, not the widget) for every subsequent await — mirrors how `prefetchProfileData`/FCM token sync already fire unawaited from inside `Auth.build()` using the auth notifier's own `ref`, never a widget's.
- **Where it applies:** Any `unawaited(...)` or fire-and-forget async call started from a `ConsumerState`/`ConsumerWidget` that is expected to keep running after navigation; `splash_screen.dart` `_bootstrap()`, `profile_provider.dart` `ProfileNotifier.autoDetectAndFillLocationIfMissing()`.

### 2026-08-04 — Global "my own X" providers must not render another entity's data
- **What happened:** A repo-wide over-engineering/profiling audit found `seller_card.dart` rendered a product's seller open/closed badge by reading the global `storeHoursNotifierProvider` — a keepAlive notifier that only ever holds the *signed-in user's own* store hours (fetched via `fetchStoreHours()` using `ref.read(authProvider)`'s own id). On a product detail screen the seller is almost never the viewer, so every product showed the viewer's own store status, not the actual seller's. Fixed by adding `sellerStoreHoursProvider` — a plain `FutureProvider.autoDispose.family<StoreHoursEntity, String>` keyed by vendor id, reusing the same `getStoreHoursUseCaseProvider` — and gating `seller_card.dart` on `authUser.id == seller.id` (own store → global notifier; anyone else → the family provider). The same class of bug recurred for `StoreHoursNotifier` itself: it's keepAlive and was never invalidated on logout, so a second vendor signing in on the same device could briefly see the prior vendor's cached hours rendered as their own — fixed with a `resetStoreHoursData(ref)` helper called alongside `resetProfileData`/`resetListingLocalCache` on every forced session clear (logout, forced 401).
- **Rule:** Any provider whose data is scoped to "the signed-in user" (not parameterized by an id) must never be read to render a *different* entity's card/screen — check `authUser.id == theEntityIdBeingShown` before reusing it, or add a proper `.family` provider keyed by that entity's id. And any such self-scoped keepAlive provider must be invalidated in the same logout/forced-401 paths as `resetProfileData`/`resetListingLocalCache`, or it leaks the previous account's data into the next session on the same device.
- **Where it applies:** `store_hours_provider.dart` (`storeHoursNotifierProvider` vs `sellerStoreHoursProvider`), `seller_card.dart`; any future global self-scoped provider (e.g. a future "my own X" notifier) reused from a widget that can render someone else's entity.

### 2026-08-04 — Sibling keepAlive notifiers need the same auth-change reset, not just the first one built
- **What happened:** Same audit: `OrdersNotifier` (consumer/shared orders) resets its state and clears `OrdersRemoteDataSourceImpl`'s static cache on every `authProvider` user change (via `ref.listen` in `build()`). Its sibling `VendorOrdersNotifier` — same feature, same datasource, same keepAlive-by-default `StateNotifierProvider` — had no such listener at all, so a vendor's order list/revenue totals stayed in memory across logout and could be seen by the next account on the same device. The fix mirrors the pattern into `VendorOrdersNotifier`'s constructor. Separately, `listing_remote_datasource.dart`'s `_localMine` offline-scaffold cache (an instance field on a `keepAlive` datasource singleton) had the identical gap — never cleared on logout, and `fetchMyListings()`'s "server returned empty → fall back to local cache" branch could surface a *different* vendor's cached drafts. Fixed by adding `clearLocalCache()` to the datasource and calling it (via a `resetListingLocalCache(ref)` helper) from the same two call sites as `resetProfileData`.
- **Rule:** When a feature has two or more keepAlive providers/notifiers reading the same session-scoped data (a "consumer" and "vendor" variant, a form notifier and its datasource cache, etc.), an auth-change/logout reset added to one of them is not proof the sibling has it too — grep every keepAlive provider in the feature and verify each one either resets on logout or is provably stateless. New session-scoped caches (static *or* instance fields on a keepAlive-scoped class) must be wired into the same central logout choke points (`Auth.logout()` in `auth_provider.dart`, `TokenRefreshInterceptor.onRefreshFailed` in `dio_provider.dart`) alongside the existing `resetProfileData(ref)` call — that's the one place guaranteed to run on every forced session clear, not a `ref.listen(authProvider)` inside a notifier that might never get built if the user never visits that screen.
- **Where it applies:** `vendor_orders_provider.dart`, `listing_remote_datasource.dart` + `listing_dependencies.dart`, `auth_provider.dart` `logout()`, `dio_provider.dart` forced-401 handler; any future feature with a consumer/vendor or list/detail pair of keepAlive providers.

### 2026-08-04 — StatefulWidget→StatelessWidget refactors leave orphaned `widget.`/state references the compiler alone won't catch mid-edit
- **What happened:** Extracting a shared `PulsingAnimationBuilder` (deduplicating ~5 hand-rolled `AnimationController` + `initState`/`dispose` pairs across `order_filter_tabs.dart`, `vendor_order_filter_tabs.dart`, `order_timeline.dart`, `notification_empty_state.dart` into one shared widget) required converting several `StatefulWidget`s to `StatelessWidget`/`ConsumerWidget`. Each conversion needed three coordinated changes — (1) drop the `State` class and its controller lifecycle, (2) rewrite every `widget.foo` reference in the old `build()` to the plain field `foo`, (3) replace the `AnimatedBuilder(animation: _controller, ...)` block with the shared widget — and doing them as separate sequential edits repeatedly left the file mid-broken between edits (a `pulse` parameter removed from one class while its only remaining usage in `build()` still referenced it; a renamed local variable `isSelected` where three other usages a few lines down still said the old name `selected`, which then silently resolved to the *widget's own field* of the same name instead of failing to compile). The review hook caught all of these before they were reported done.
- **Rule:** When converting a Stateful→Stateless/Consumer widget (or extracting a controller into a shared wrapper), make the full set of coordinated changes — state-class removal, `widget.x`→`x` rename throughout `build()`, and the controller-usage replacement — in as few edits as possible, then **re-read the entire file** before considering the file done. Grep the file for `widget\.` and the old controller/field name afterward; a stray hit either means a missed rename or, worse, a rename that now silently resolves to a same-named field/class member instead of erroring.
- **Where it applies:** Any Stateful↔Stateless conversion; any refactor that renames a local variable to a name that could collide with an enclosing class field or widget property.

### 2026-08-04 — "Go to Home after auth" has 9 call sites across 5 screens, not one
- **What happened:** The location-rationale popup (above) was wired only into `SplashScreen._bootstrap()`'s `user != null` branch, on the assumption that was "the" entry point to the app after auth, matching the user's own phrasing ("splash screen, before routing"). The user reported the popup wasn't showing. Grepping `context.go(AppRoutes.home)` found it fires from 9 places across 5 screens: `splash_screen.dart` (cold-start restore only), `login_screen.dart` (password login, OTP-via-sheet login, guest-mode enable), `otp_screen.dart` (already-authed guard in `initState`, inline OTP field completion, verify button), `register_screen.dart` (vendor success overlay dismiss), `courier_login_screen.dart` (password/OTP login). Splash only covers the case where a session already existed before the app launched — every *fresh* login/register/OTP-verify/courier-login bypasses splash entirely and never saw the popup. This is the exact same shape as the documented guest-mode lesson below ("The route redirect is a safety net, not the guest UX") — one central hook missed the real user-facing entry points.
- **Rule:** Any "do X once when the user enters the authenticated app" feature must be triggered from *every* `context.go(AppRoutes.home)` (or equivalent) site reachable after a successful auth action, not just splash's cold-start-restore branch. Extract the behavior into one shared `shared/utils/` function (mirrors `require_login.dart`'s "gate at every entry point" pattern) and call it at each site right before the navigation, with a `mounted`/`context.mounted` guard after every `await` in between — never assume splash is "the" post-auth funnel.
- **Where it applies:** `location_permission_prompt.dart` (`maybeShowLocationPermissionPrompt`), and any future first-run/onboarding-style prompt gated on "user just became authenticated" — check `login_screen.dart`, `otp_screen.dart`, `register_screen.dart`, `courier_login_screen.dart`, `splash_screen.dart` for every `AppRoutes.home` navigation before assuming coverage.

### 2026-08-04 — Release APK build broken: `flutter_local_notifications` needs core library desugaring, not enabled in `android/app/build.gradle.kts`
- **What happened:** The CI "Build and Release APK" workflow (`.github/workflows/build-and-release-apk.yml`) started failing at the `flutter build apk --release` step with `Execution failed for task ':app:checkProdReleaseAarMetadata' ... Dependency ':flutter_local_notifications' requires core library desugaring to be enabled for :app`. `android/app/build.gradle.kts`'s `compileOptions` set `sourceCompatibility`/`targetCompatibility` to Java 11 but never set `isCoreLibraryDesugaringEnabled = true`, and the `dependencies` block had no `coreLibraryDesugaring(...)` artifact — `flutter_local_notifications` (18.0.1, added for FCM local notifications per the 2026-08-03 FCM lesson above) requires desugaring for its Java 8+ API usage on `minSdk` below 26, and Gradle's `CheckAarMetadataWorkAction` enforces this at the AAR-metadata-check stage, before any Kotlin/Dart compilation runs — so `flutter analyze`/`flutter test` never catch it, only a real `flutter build apk`/`appbundle` does.
- **Rule:** Any plugin that requires core library desugaring (check the error text — Gradle names the exact dependency) needs both: `isCoreLibraryDesugaringEnabled = true` inside `android { compileOptions { ... } }`, and `coreLibraryDesugaring("com.android.tools:desugar_jdk_libs:<version>")` in the app module's `dependencies` block (version per the plugin's own requirement — 2.1.4+ for recent `flutter_local_notifications`). Since this class of failure only surfaces during an actual Gradle assemble/bundle task (not analyze/test/pub get), verify any new Android-plugin dependency with a real `flutter build apk` (or check CI) before considering the change done, not just `flutter analyze`.
- **Where it applies:** `android/app/build.gradle.kts`; any future plugin addition that touches Android and mentions "desugaring" in its own README/error output.
- **Fixed 2026-08-04, second gap found:** Even after fixing the 9-site coverage above, the user reported the popup still didn't show on a fresh uninstall+reinstall. Root cause: the fix still only called `maybeShowLocationPermissionPrompt` from splash's `user != null` branch — but a fresh install has NO session, so it hits the `onboarding`/`login` branch instead and the call was never reached at all. The mistake was conflating two different "first time" meanings: I'd designed this around "first time the user is authenticated" (matching the earlier answer that the feature's purpose is auto-filling the *profile*), but the user's actual phrasing — "first time will show a popup ... when the user open[s] the app" — means first app launch ever, independent of login state. **Rule:** when a feature description says "first time the app opens" or "when the user enters the app," don't silently narrow that to "first time authenticated" just because the payoff (profile auto-fill) needs a session — the trigger and the payoff can have different gates. Moved the call in `splash_screen.dart` `_bootstrap()` to run unconditionally before the `user != null` branch, so the rationale popup shows on the very first cold start regardless of auth state; `autoDetectAndFillLocationIfMissing()` still no-ops internally without a session, so the profile-fill payoff still only applies post-login (via the same call already wired into the 9 auth-success sites).

### 2026-08-04 — A missing 404 fallback isn't a live bug until the call chain is traced from the actual screen
- **What happened:** Flagged `AuthRemoteDataSource.loginWithPhoneToken` as broken (no `LegacyRouteOptions.allowNotFound()` fallback, unlike its sibling `loginWithSocialToken`) because the backing route is unconfirmed/undeployed — reasonable from the datasource alone. But tracing the real phone-OTP screen (`otp_screen.dart` → `phoneAuthProvider`/`PhoneAuthNotifier.verifyOtp()`) showed it calls `loginWithOtpUseCaseProvider` → `ApiEndpoints.loginWithOtp`, a CONFIRMED live backend route — a completely different phone-auth mechanism. `loginWithPhoneToken` is only reachable via `AuthRepository.verifyOtp` → `VerifyOtpUseCase` → `verifyOtpUseCaseProvider`, which a repo-wide grep for `VerifyOtpParams(`/`SendOtpParams(` construction showed has zero call sites anywhere in `lib/` — fully dead code, same shape as the already-known-unused `RegisterUseCase`. The social-login sibling (`loginWithSocialToken`, same missing-fallback shape) IS live — reachable from `apple_sign_in_button.dart`/`facebook_sign_in_button.dart` via `socialAuthProvider`.
- **Rule:** Before reporting a datasource method as a live bug (missing fallback, wrong contract, etc.), trace its full call chain from an actual screen/widget down (screen → provider/notifier → usecase provider → repository → datasource method), not just from the interface method up. Two datasource methods with near-identical names/shapes (`loginWithPhoneToken` vs `loginWithSocialToken`) can have completely different reachability — one dead, one live — and grepping `SomeParams(` construction repo-wide is a fast way to confirm a usecase path is genuinely unreachable before "fixing" it.
- **Where it applies:** Any bug report/fix targeting a datasource or repository method; this repo already has one other confirmed-dead auth path (`register`/`RegisterUseCase`) worth checking before touching `auth_remote_datasource.dart`.

### 2026-08-04 — tempurl.com free-trial host rotates; grep-and-diff isn't enough, ask the user which is current
- **What happened:** User reported "Home not getting data" (infinite loading spinner). Found two different hardcoded API origins in the repo: `https://xstoreegy-001-site1.jtempurl.com` (in `Taskfile.yml`, `build-and-release-apk.yml`, probe scripts, and an older lesson entry) vs `https://xstoreegy002-001-site1.etempurl.com` (only in `api_endpoints.dart`'s debug fallback). Assumed the majority/documented one (`jtempurl.com`) was correct and "fixed" the fallback to match it. The user then corrected this: `etempurl.com` is the CURRENT live host — `jtempurl.com` is a prior free-trial instance that expired (`tempurl.com` hosting trials are time-boxed and get replaced, not renewed). The user also supplied a new Basic license key (`Basic MTEzMjQ4ODM6NjAtZGF5ZnJlZXRyaWFs`, decodes to `11324883:60-dayfreetrial`) to replace the old one (`Basic MTEzMTk3Njg6NjAtZGF5ZnJlZXRyaWFs`, decodes to `11319768:60-dayfreetrial`) — same shape, different trial-account ID, confirming a full host+credential rotation rather than a typo either way.
- **Rule:** A hardcoded `tempurl.com`-style free-trial host/credential pair is not a fact to infer from internal consistency (majority of references, "documented" comments) — it expires on its own schedule independent of this repo. When two conflicting hosts/keys exist, or "not getting data" could be a stale-host symptom, ask the user which is current rather than picking the one more files agree on. When the user gives a new host or key, update every hardcoded occurrence together (`api_endpoints.dart` baseUrl fallback, `api_auth_headers.dart` basicLicenseKey, `Taskfile.yml` API_BASE_URL_DEV/PROD defaults + comment, `build-and-release-apk.yml` env defaults + comment, `scripts/probe_*.py` BASE/AUTH) — a partial swap reintroduces the same "two hosts disagree" bug.
- **Where it applies:** `api_endpoints.dart`, `api_auth_headers.dart`, `Taskfile.yml`, `.github/workflows/build-and-release-apk.yml`, `scripts/probe_*.py` — anywhere the API origin or Basic license key is hardcoded.

### 2026-08-05 — A field-by-field Postman diff finds bugs a folder-by-folder endpoint audit misses
- **What happened:** A full re-audit of auth/profile/listing/home/explore against a freshly-handed-over Postman collection (previous audits, e.g. the 2026-08-03 endpoint audit, only diffed routes and body *shape*) found three live wire-key/param bugs that shape-level checks had missed: (1) `registerVendor`'s formdata sent `storeGovernmentId`, but the collection's own example explicitly uses `storeGovernorateId` — same value, wrong key, so the backend likely drops the vendor's store governorate silently on every signup (extra/misnamed multipart keys don't 400, they just don't bind). (2) `verifyEmailOtp(String otpToken)` / `verifyPhoneOtpBackend(String otpToken)` only ever sent `otpToken` — the collection's example bodies for both are `{email, otpToken}` / `{phoneNumber, otpToken}`, and the method signatures didn't even have a parameter to carry the missing field. Traced the call chain (per the "trace before flagging" lesson above) and confirmed both use-case providers have zero call sites outside the generated file — dead code today, but would 400 immediately if ever wired into a verification screen. (3) Explore's category filter never sent the confirmed `categoryId` query param at all — `filter_bottom_sheet.dart` hardcoded 4 category labels disconnected from the real `/api/categories` catalog, and matching happened entirely client-side against already-fetched pages.
- **Rule:** When reconciling against a Postman collection, diff at the **field-name level**, not just the endpoint/method/shape level — a `POST`/multipart write can have the exact right route and field count and still silently drop data because one key is misspelled relative to the backend's actual model property name (`...Government...` vs `...Governorate...` is exactly this kind of near-miss). For every write endpoint, walk the collection's example body key-by-key against the Dart call site's map/FormData keys. Do the same for confirmed-but-unused query params (the collection documenting a param the app never sends is itself a finding, not just a shape mismatch) — check both what's sent and what's available-but-unsent.
- **Where it applies:** Any future reconciliation against a Postman/OpenAPI collection; `auth_remote_datasource.dart` (registerVendor, verifyEmailOtp, verifyPhoneOtpBackend), `explore_provider.dart`/`explore_remote_datasource.dart` (categoryId).

### 2026-08-06 — A second backend needs a second Dio client and its own session bridge, not a reused one
- **What happened:** Wiring `DeliveryRequestRemoteDataSource` to the standalone delivery-backend service (separate host, `Authorization: Bearer` JWT, no `X-Auth-Token`/Basic-license scheme) required a parallel stack to the main `dio_provider.dart`: a second keepAlive Dio (`delivery_dio_provider.dart`), its own token storage key (`PrefsKeys.deliveryAuthToken`, not `authToken`), and — since delivery-backend has no real identity provider, only a dev-only phone login — a silent bridge (`delivery_backend_session.dart`) that exchanges the app's real xStoreEcommerce session for a delivery-backend token right after auth success, auto-provisioning consumer/vendor identities there (never courier/admin, matching the app's own self-registration rules).
- **Rule:** A second backend/microservice is never "just point the existing Dio at a new baseUrl" — different auth scheme, different token lifecycle (no refresh endpoint here, so re-running the bridge login IS the refresh, triggered from the Dio's own 401 interceptor), different failure mode (bridge failure must be silent/best-effort and never block the main auth flow). Wire its session bridge into the SAME three choke points as `syncFcmDeviceTokenWithBackend`/`prefetchProfileData` (`Auth.build()` cold-start restore, `setUser`, `adoptSession` in `auth_provider.dart`) and clear it in `logout()` — that's the established, reviewed-safe pattern for "do X once when a session starts" in this repo; don't invent a new call site.
- **Where it applies:** Any future second/external backend integration; `delivery_dio_provider.dart`, `delivery_backend_session.dart`, `auth_provider.dart`.

### 2026-08-06 — Real wiring only makes sense as a coherent unit — check upstream dependencies before wiring downstream reads
- **What happened:** Asked to wire Orders (Flow A) to the real delivery-backend, found `_remote.placeOrder(params)` in `cart_repository_impl.dart` builds a complete `OrderEntity` — including client-computed `shippingCost` — entirely in the mock `cart_remote_datasource.dart`, with no backend at all. The new delivery-backend checkout endpoint always recomputes `shippingCost` server-side and ignores whatever the client sends, so wiring just the READ side (order list/detail/accept) while checkout keeps creating client-only mock orders would leave two disconnected data sources: real endpoints querying an empty database, mock-created orders invisible to them, and a checkout screen showing a price the server would silently overrule if it ever got called. Scoped down to wiring only Flow B (delivery requests), which has no such upstream dependency — full CRUD already existed against a real, seeded backend.
- **Rule:** Before wiring a datasource to a real backend, trace what CREATES the data it reads, not just whether the read/write endpoints exist. If the creation path is a separate, unwired mock feature (here: cart/checkout) whose own price computation would be silently overridden by the real endpoint's authoritative computation, wiring only the downstream reads produces a worse, half-real state than leaving the whole feature mocked — flag the upstream gap and scope the wiring to what's actually self-contained, rather than a partial wire that looks done but is internally inconsistent.
- **Where it applies:** Any future "wire X to the real backend" task where X has an upstream creation path owned by a different, still-mocked feature; specifically `cart_repository_impl.dart`/`cart_remote_datasource.dart` before Orders (Flow A) can be wired for real.

### 2026-08-05 — Home's "New Arrivals" and "Recommended" are the same fetch as "Hot Deals", just re-sorted
- **What happened:** Auditing Home against the Postman collection found `HomeRepositoryImpl.getNewArrivals()` and `getRecommended()` don't call the network at all — both internally call `getHotDeals()` (the single `GET /api/listings?page=1&pageSize=40` derivation) and just re-sort/relabel the same 40 listings. Not a wire-contract bug (no wrong endpoint/fields — the collection genuinely has no dedicated endpoints for these), but a product-visible gap: all three home carousels draw from one overlapping pool and can show near-duplicate items on a catalog bigger than 40 listings.
- **Rule:** When an audit finds multiple UI sections backed by literally the same fetch (not just the same *endpoint* — the same in-memory result, re-sorted), flag it explicitly even if no single endpoint call is "wrong" — a contract-level PASS can still hide a product-level gap. Worth a backend ask (dedicated new-arrivals/recommended endpoints or sort params) rather than a client-side fix, since there's no more real data to derive it from client-side.
- **Where it applies:** `home_repository_impl.dart` `getNewArrivals`/`getRecommended`; any future home-screen section audit.

### 2026-08-06 — Background telemetry needs its own Dio client, and free-text UI strings must not leak into analytics properties
- **What happened:** Building the analytics event service (`lib/core/analytics/analytics_service.dart`), two things surfaced during self-review before they shipped: (1) the shared `dio` provider's error interceptor flips the whole app to the full-screen server-error state on any 5xx (`dio_provider.dart`'s `onError` → `serverErrorProvider.trigger()`) — reusing it for a background telemetry POST would mean a flaky analytics endpoint takes over the UI, which is never acceptable for a fire-and-forget background call. (2) The first draft of `login_prompt_shown` tracking in `require_login.dart` passed the function's `message` parameter as an analytics property — but `message` is localized UI copy (`context.l10n.signInToWriteReview`), not a stable categorical dimension; it would have bloated event cardinality with locale-dependent free text that can't be aggregated in a dashboard funnel.
- **Rule:** Any background/telemetry HTTP client must be a separate `Dio` instance from the shared API client if the shared client has side effects (global error UI, forced-logout interceptors, etc.) that are correct for user-facing calls but wrong for a fire-and-forget background call — don't reuse it just to avoid "a second Dio instance." Never pass a localized/user-facing string (anything from `context.l10n.*` or free-form UI copy) as an analytics property value — only pass stable, enum-like identifiers; if you need "where," use structural context that's already attached (e.g. this service's auto-tracked `screenName`), not the string shown on screen.
- **Where it applies:** `analytics_service.dart` and any future telemetry/logging pipeline; any `track()`/analytics call site across the app — check every property value traces back to an enum, id, or number, never a `context.l10n.*` string.

### 2026-08-06 — "Backend needs a small change" claims must be verified by grepping the actual backend repo, not inferred from the mobile-side contract comments
- **What happened:** Asked to "complete the cycle" for the vendor delivery-method (self/platform) confirm flow, assumed from `api_endpoints.dart`'s own comments ("Legacy orders module — not on the confirmed `/api` contract... until the backend ships") that the backend mostly had an Orders module and just needed a `deliveryMethod` field added to an existing confirm endpoint. Grepping `~/repos/xStoreEcommerce` directly found there was no Order entity, no migration, no controller, no command — Orders had never been built server-side at all; the mobile Orders feature (and its comments) had been written entirely speculatively against a contract nobody had implemented. Same shape of gap on the admin-dashboard side: the "Assign courier" UI looked wired (real courier list from delivery-backend) but `assignCourier()` only mutated a local in-memory array — no backend call at all.
- **Rule:** Before scoping a "just needs a small backend addition" task, `grep`/`find` the actual backend repo(s) for the entity/controller/endpoint in question — never take a mobile-side comment, a memory note, or "the UI looks wired" as proof the backend exists or does what's assumed. When the backend turns out to not exist at all (not a field/endpoint gap but a whole missing domain), stop and re-scope with the user before building — "add a field" and "build the entity/migration/CQRS stack/controller from scratch" are different-order tasks that deserve an explicit go/no-go, not silent absorption into the original small ask.
- **Where it applies:** Any task framed as "the backend needs X" or "just wire Y to the real API" — verify against the backend repo's actual source before estimating scope; `api_endpoints.dart`'s "legacy"/"not confirmed" comments are themselves unverified claims, not established fact.

### 2026-08-07 — A `ref.listen(authProvider, ...)` fetch-on-login pattern misses the transition when the provider is built after login already resolved
- **What happened:** Manual end-to-end QA of the order+delivery flow under `--dart-define=MOCK=true` found the cart badge showed 0 items immediately after a fresh login, despite `cart_remote_datasource.dart` pre-seeding 3 mock items on first `getCart()`. Root cause: `Cart.build()` in `cart_provider.dart` registers `ref.listen<AsyncValue<UserEntity?>>(authProvider, (prev, next) { ... fetchCart(); })` to load the cart on auth changes, but `Cart` is `@Riverpod(keepAlive: true)` — never eagerly instantiated, only built the first time something calls `ref.watch(cartProvider)` (here, `home_screen.dart`, which only renders *after* login already resolved). By the time `build()` runs and registers the listener, the login transition already happened; `ref.listen` doesn't fire for the current value, only future changes — so the initial fetch is silently skipped. A second in-session auth transition (logout → login again, with the provider now already alive) fires the listener correctly and loads the cart. `orders_provider.dart` uses the identical shape and likely has the same gap.
- **Rule:** A `ref.listen(authProvider, ...)`-triggered fetch is not sufficient on its own for "load this on login" when the listening provider is lazily built (the normal case for anything not read at app root) — the provider can come alive strictly after the transition it needs to react to. Either seed the initial fetch from the auth state already present in `build()` (read it directly, don't rely solely on future listen callbacks) or verify the provider is guaranteed to be alive before any possible login transition. When auditing/fixing one, check every sibling with the same `ref.listen(authProvider) → fetch` shape (grep for the pattern) rather than fixing the one you found.
- **Where it applies:** `cart_provider.dart` `Cart.build()`, `orders_provider.dart`, `vendor_orders_provider.dart` — any keepAlive notifier that fetches session data reactively off an auth listener instead of eagerly.

### 2026-08-07 — Mock-mode coverage is per-datasource, not per-feature — a feature can be "mocked" for writes/lists and still hit the live backend for reads
- **What happened:** Same QA pass: with `MOCK=true`, Home/Cart/Orders/Delivery all serve seeded mock data as expected, but tapping any product card hit `product_remote_datasource.dart` (zero `MockConfig` references anywhere in `lib/features/product/`) which called the real, currently-404ing live backend and threw a hard error screen — even though the cart the product would be added to is fully mocked. Same gap in `explore_remote_datasource.dart`: `getSuggestions()` (typeahead) has a `MockConfig.useMock` branch, but the actual `searchListings()` that powers every Explore/search result list does not, so Explore always shows "0 results" under mock mode regardless of query. In both cases the *feature* reads as "supports mock mode" because sibling datasources (cart, orders) do, but the specific datasource file handling that screen was never wired.
- **Rule:** "Does this feature support mock mode" is not a per-feature yes/no — grep `MockConfig` inside the *specific* datasource file backing the screen in question before assuming coverage, especially for read paths (single-item detail, search/list results) that are easy to skip when only the write/mutation paths (add-to-cart, place-order) were built out first. When adding a new mock branch to one method of a datasource, check every other public method on that same interface for the same gap in one pass, not just the one currently failing.
- **Where it applies:** `product_remote_datasource.dart` (no mock branch at all), `explore_remote_datasource.dart` `searchListings()` (mock branch exists on `getSuggestions()` only) — check both before relying on mock mode to browse products end-to-end; any future datasource audit should enumerate every interface method, not just the one being changed.

### 2026-08-11 — Re-probe location reference data when the cascade goes empty
- **What happened:** Buyer register government/city popup showed no cascade data because the app still filtered the old inverted hierarchy (`governments` by `cityId`) after the live API flipped to governorate→city (`cities.governorateId`).
- **Rule:** When a location/government/city picker shows empty or wrong lists, re-probe `/api/governorates` and `/api/cities` shapes before trusting prior lessons or Postman examples — hierarchy and parent-link key names have already drifted once. Cascade must be parent=`allGovernments`, child=`allCities` filtered by `governorateId`.
- **Where it applies:** `location_cascade_field.dart`, city/government models and datasources.

### 2026-08-14 — Backend error envelope changed to `{isSuccess, data, errorEn, errorAr, statusCode}` and the app-wide mapper never read it
- **What happened:** A fresh Postman collection audit + live probe found every backend error response (400/401/403/404) now returns `{"isSuccess": false, "data": null, "errorEn": "...", "errorAr": "...", "statusCode": ...}` — but `dio_error_mapper.dart`'s `_serverErrorMessage` only checked `error`/`message`/`error.message` (the 2026-07-11 "surface API error bodies" contract), which this envelope doesn't use. Every backend validation/business-rule error across the ENTIRE app — not just one feature — was falling through to generic Dio boilerplate ("Http status error [400]") instead of the actual actionable, already-translated backend text (e.g. "Account must be verified to create listings.", "Location data is required...").
- **Rule:** A confirmed error-body contract is a snapshot, same as any other backend contract (see the 2026-08-02 Postman-supersedes lesson) — it can be replaced wholesale, not just have fields added. When live-probing turns up a DIFFERENT error envelope shape than what a mapper currently reads, add the new key(s) as additional candidates rather than assuming the old ones still apply; `_serverErrorMessage` now checks `errorEn` first, then the legacy `error`/`message`, then `errorAr`.
- **Where it applies:** `dio_error_mapper.dart` `_serverErrorMessage` — re-verify this envelope shape periodically; it has already changed once.

### 2026-08-14 — GET /api/listings now requires X-Latitude/X-Longitude headers, app-wide, and nothing sent them
- **What happened:** Live probe (`curl` against the hosted backend with only the standard Basic license header) found `GET /api/listings` unconditionally 400s: `"Location data is required. Please provide X-Latitude and X-Longitude headers."` — a requirement that didn't exist when the endpoint was last confirmed. Since Explore, Home's derived hot-deals/new-arrivals/recommended, and Similar Products all read through this one endpoint, this was a full-catalog outage in live mode, not a feature-scoped bug — and it predates and is unrelated to the Orders/Home work this session was scoped to; it was only caught by probing before writing new code that also depends on listings data.
- **Rule:** Added `AppLocationCache` (`core/utils/app_location_cache.dart`) — a static last-known lat/lng, primed from `Geolocator.getLastKnownPosition()` (no permission prompt) at `bootstrap()`, kept current by `LocationService.getCurrentLocation()`'s existing success path, falling back to Cairo coordinates so location-gated endpoints never hard-fail before a fix is available. Attached as `X-Latitude`/`X-Longitude` on every request in `dio_provider.dart`'s existing `onRequest` interceptor (same place `X-Auth-Token` is attached) — never re-derive location per call site. When a previously-unconditional endpoint starts 400ing across the board, check for a new required header before assuming a payload/auth regression.
- **Where it applies:** `dio_provider.dart`, `app_location_cache.dart`, `location_service.dart`; any future endpoint that turns out to need location context.

### 2026-08-14 — Register password now requires upper+lower+digit+special char; the Postman example ("123456") no longer passes
- **What happened:** Live probe of `POST /api/auth/consumer/register` with the collection's own example password (`"123456"`) returned 400: `"Password must be at least 8 characters and include uppercase, lowercase, digit, and special character."` — a stricter rule than any previously confirmed contract, and stricter than the collection's own example body satisfies.
- **Rule:** A Postman collection's example values are illustrative, not necessarily currently valid — live-probe with a value that satisfies what you'd actually expect production validation to require, not just whatever the example shows, especially for password/security-adjacent fields. Check `register_screen.dart`/`auth_provider.dart` client-side password validation against this rule before shipping (not verified as part of this session's scope — flagged for the required-for-production audit).
- **Where it applies:** Register screen client-side validation, `Validators` for password strength; re-verify before any register-flow change.

### 2026-08-14 — Real Orders/Vendor-Orders contract: single listing per order, bulk status endpoint, stats embedded in the list response
- **What happened:** A fresh Postman collection showed the backend now has a real Orders module (it didn't exist as of the 2026-08-06 lesson above) — but modeled nothing like the mock-based `OrdersRemoteDataSource` the app had built speculatively. Live-probed and confirmed: `POST /api/orders` takes exactly `{listingId, quantity, latitude, longitude}` — one listing per order, no multi-item/cart endpoint exists anywhere. `GET /orders/me` returns a bare array. `GET /vendor/orders` returns `{orders, totalCount, pendingCount, confirmedCount, totalRevenue, warnThresholdEgp, pauseThresholdEgp, exceedsWarnThreshold, exceedsPauseThreshold, commissionValueOnOrder}` — vendor stats are embedded in the same call, no separate stats endpoint exists. Status changes go through one bulk `PUT /api/vendor/orders/status {orderIds:[...], status:"..."}` — not the separate confirm/reject/processing/shipped/delivered routes the app assumed, none of which exist. UNCONFIRMED and NOT resolved this session: the field shape of a single order object (no example response in the collection) and the full status enum beyond `"confirmed"` — the live probe couldn't reach real order data because the test vendor account needed admin approval before it could list a product, which blocks the natural next step (create a listing → place an order → inspect the response).
- **Rule:** Rewired `OrdersRemoteDataSourceImpl` against the confirmed routes/request bodies with tolerant, clearly-commented parsing for the unconfirmed parts (per-order field shape, full status enum) rather than blocking on a probe that hit a real access-control wall. `CartRemoteDataSourceImpl.placeOrder` now creates one real order per cart line (via `OrdersRemoteDataSource.createOrder`) since there's no batch endpoint — documented as a stopgap, not a true atomic multi-item order. `confirmOrder`'s delivery-method choice and `markShipped`'s tracking info are applied locally for display only; no wire field exists for either. Before this ships to production: get the vendor a real listing (admin-approve a test vendor, or ask the backend team for a seeded order) and confirm the actual order-object shape and full status enum against real data — everything here is confirmed-request/tolerant-response, not confirmed end-to-end.
- **Where it applies:** `orders_remote_datasource.dart`, `orders_repository_impl.dart` (`getOrderDetail` now branches: vendor sessions search `getVendorOrders` since no vendor-scoped by-id route exists), `cart_remote_datasource.dart` `placeOrder`, `api_endpoints.dart` Orders section.

### 2026-08-14 — Full order lifecycle live-probed with the seeded test vendor account; two new business rules confirmed, one new admin-only wall found
- **What happened:** Given the seeded vendor credentials (`01112345678` / `P@ssw0rd`), pushed the end-to-end probe from the lesson above further. Sequence that actually worked: (1) `POST /api/listings` 403'd with "Account must be verified to create listings" even for this pre-seeded account — `get-profile` showed `isPhoneVerificationRequired: true` (email OTP is broken — `send-email-otp` 500s with an SMTP auth error, matching the known `backend-live-defects-2026-08` memory entry — but `send-phone-otp` works and echoes the OTP); `POST /api/auth/verify-phone` cleared it. (2) Listing creation THEN 403'd with a different message — "Store location (latitude/longitude) must be set before creating a listing" — `PUT /api/auth/update-profile` (multipart) with `lat`/`lng` fields fixed it, but only after also resending `birthDate` in the SAME multipart call: omitting it 400s with `'Request Birth Date' must not be empty`, reconfirming the existing "update-profile is full-replace, not a patch" lesson applies to fields well beyond birthDate itself. After both fixes, `POST /api/listings` returned 201 with a full listing object (real field-shape confirmed: `id` is an int, `status:1` with `reviewedAt:null` — pending admin review). (3) A freshly-registered CONSUMER hit `POST /api/orders` → 400 `"Please verify your phone number before placing an order"` — a brand-new, previously-undocumented business rule; the app's checkout flow has no phone-verification gate anywhere today, so a real unverified consumer would hit this 400 with no warning. Verifying the consumer's phone (same `send-phone-otp`/`verify-phone` flow) cleared that gate too. (4) Final wall: `POST /api/orders` against the newly-created listing (id=1) 400'd with `"The listing is not available"` — the listing's `status:1`/`reviewedAt:null` means it's pending admin approval (`PUT /api/admin/listings/{id}/approve`), a genuinely admin-only gate with no self-service path. No admin credentials were available, so the final response shape of a real order (from `GET /orders/me/{id}` post-purchase) and the full vendor status enum remain unconfirmed — this is the one gap in the audit that requires an actual admin account, not more probing.
- **Rule:** Before assuming an "account must be verified" or "not available" error is an admin-approval wall, check whether it's actually a *self-service* gate first (phone/email OTP, missing store lat/lng) — two of the three walls hit here were self-service and only one was genuinely admin-only. When probing a write flow gated behind multiple preconditions, expect them to surface one at a time (fix one 403, hit the next) rather than all at once. Checkout/place-order flows must handle "phone not verified" as a distinct, actionable error state (route to OTP verification) rather than a generic failure toast — this is unhandled in the app today.
- **Where it applies:** Register/verification screens (phone OTP is required before ordering, not just optional profile completion), `place_order`/checkout error handling (needs a specific case for the "verify your phone" 400), vendor onboarding (store lat/lng is a hard precondition for listing creation, not just a nice-to-have), any future live-probe of a gated write flow — expect layered preconditions, not one wall.
- **Fixed 2026-08-14:** Both gaps closed the same session. `dio_error_mapper.dart` now special-cases these two error texts into stable codes (`phoneNotVerifiedErrorCode`, `storeLocationRequiredErrorCode` in `app_error_messages.dart`) rather than showing raw server text — mirrors the existing `rateLimitErrorCode` pattern. Checkout (`checkout_screen.dart`) detects `phoneNotVerifiedErrorCode` and opens a new self-service sheet (`phone_verification_sheet.dart`, `verifyPhoneNow()`) that sends/verifies an OTP via the already-built-but-previously-dead-code `sendPhoneOtpBackendUseCase`/`verifyPhoneOtpBackendUseCase`, then retries `placeOrder()` automatically on success. Add Listing (`add_listing_screen.dart`) detects `storeLocationRequiredErrorCode` and shows a "Set location" snackbar action that pushes `AppRoutes.profileEdit` (a plain `GoRoute`, safe to `context.push`) — the vendor still has to tap "detect location" + save there themselves; this doesn't auto-fill it inline. One bug caught in self-review before shipping: `CheckoutNotifier.placeOrder()`'s failure branch was building its user-facing error from its OWN previous state (`state.error ?? 'failed'`) instead of reading the specific code `Cart.placeOrder()` had just written to `CartState.error` — every checkout failure was silently collapsing to a generic `'failed'` message. Fixed by reading `ref.read(cartProvider).error` at the point of failure.
- **Where it applies:** `checkout_provider.dart`, `checkout_screen.dart`, `phone_verification_sheet.dart` (new), `add_listing_screen.dart`, `listing_form_notifier.dart`, `dio_error_mapper.dart`, `app_error_messages.dart` — the `Cart`-notifier-error-not-propagating-to-`Checkout`-notifier bug pattern is worth checking for in any other two-notifier composition (a notifier that wraps another notifier's mutation and only checks `result == null` without reading the wrapped notifier's own error state).

### 2026-08-14 — PUT /orders/{id} (delivery-coordinate update) wired end to end
- **What happened:** The only remaining confirmed-but-unwired Orders route (flagged in the production-readiness audit as "route added, no screen calls it") got a real caller: consumer order detail, for `pending`/`confirmed` orders, now has an "Update delivery location" action alongside Cancel. It re-runs `LocationService().getCurrentLocation()` (same GPS-detect flow as edit-profile) and calls the new `UpdateDeliveryLocationUseCase` → `OrdersRepository.updateDeliveryLocation` → `OrdersRemoteDataSourceImpl.updateDeliveryCoordinates` (`PUT /orders/{id}`, body `{latitude, longitude}`, response never live-probed — treated as bodyless-success, matches the `POST /orders/{id}/cancel` precedent of no documented response body). Deliberately did NOT add lat/lng fields to `OrderEntity`/`OrderAddress` — nothing in the app displays a delivery pin, so storing coordinates the UI never reads would be dead state; the action's own state field (`OrderDetailState.error`) is enough to drive a success/error snackbar.
- **Rule:** When a confirmed-but-orphaned endpoint gets a real UI trigger, resist the urge to also thread its data through the domain entity "for completeness" if nothing displays it — that's the over-engineering CLAUDE.md already bans, just approached from the data-modeling side instead of the abstraction side. `LocationService`'s three location exceptions (`XStoreLocationServiceDisabledException`/`XStoreLocationPermissionDeniedException`/`XStoreLocationPermissionPermanentlyDeniedException`) map to the SAME two `locationServiceDisabled`/`locationPermissionDenied` l10n-keyed error codes already established by `profile_provider.dart`'s `detectCurrentLocation()` — reuse those exact string codes (and their existing l10n strings) rather than inventing new ones for the same two OS-level conditions.
- **Where it applies:** `order_detail_provider.dart` (`updateDeliveryLocation`), `order_action_buttons.dart`, `orders_remote_datasource.dart`, `orders_dependencies.dart`; any future feature reusing `LocationService`'s GPS-detect flow should reuse these same two error codes.

### 2026-08-15 — Launch checkout is COD-only; Coming Soon routes must not stay tappable
- **What happened:** Checkout still collected PAN/CVV despite a COD-only launch, and profile/order CTAs still opened Coming Soon stubs (or WhatsApp'd `01…` digits that `wa.me` rejects).
- **Rule:** Never collect card PAN/CVV on a COD-only checkout — keep historical `PaymentMethod` enum members only for parsing old/mock orders. Hide or redirect unfinished menu/routes instead of leaving a Coming Soon screen behind a tap. Open WhatsApp through `whatsAppDigits`/`launchWhatsApp` (Egypt `01…` → `20…`); don't strip non-digits and pass a local number to `wa.me`.
- **Where it applies:** Checkout payment step/`PlaceOrderParams`, profile menu + router redirects, product/store/order WhatsApp CTAs (`lib/shared/utils/whatsapp.dart`).

### 2026-08-15 — Don't invent seller stats, stock, or ship mock to production
- **What happened:** Missing API rating/sales/stock defaulted to 4.9 / 230 / 99, and `MOCK=true` plus `localhost:5080` could still compile into a release APK.
- **Rule:** Omit rating/sales when the API omits them and label "New seller" — never fabricate marketplace reputation. Missing stock is 0, not 99. `MockConfig.useMock` must be false in `kReleaseMode` even if `MOCK=true` was passed; delivery base URL must not fall back to localhost in release. Don't publish unfilled `{{PLACEHOLDER}}` legal templates as live Terms — ship an operational draft with an honest banner, and disclose GPS headers / no card collection as the app actually behaves.
- **Where it applies:** `ProductSellerEntity`, product detail stock, `mock_config.dart`, `delivery_api_endpoints.dart`, Terms/Privacy screens.

### 2026-08-15 — Platform commission is flat EGP per order, not a %
- **What happened:** Starter commission was modeled as 2% of price; product intent is a flat 2 EGP fee per order.
- **Rule:** Use `kStarterCommissionFeeEgp` + `commissionFeeEgpForCategory` and `CommissionBreakdown.forPrice(..., feeEgp:)` — never `price * rate/100` or a `%` label on commission UI. Changing the fee model means updating domain, config, listing preview, order breakdown, and wallet sum together.
- **Where it applies:** `lib/features/commission/`, add-listing breakdown card, vendor order price breakdown, vendor commission wallet.

### 2026-08-15 — "Don't invent seller stats" fix must chase the fabricated value through every entity that carries it, not just the one the ask named
- **What happened:** Team-lead review of the COD-launch + no-fabricated-stats diff found `ProductSellerEntity.rating`/`salesCount` were correctly made nullable with a "New seller" fallback, but three sibling entities carrying the SAME concept under the SAME name — `CartEntity.vendorRating`, `CartItemEntity.vendorRating`, `OrderEntity.vendorRating` — still default to `4.8` and get actively backfilled by `orders_remote_datasource.dart` (`?? 4.8` on real API data) and `cart_remote_datasource.dart` (`sellerRating == 0 ? 4.7 : sellerRating`), then render unconditionally as `⭐ 4.7`/`⭐ 4.8` on cart and order-detail screens. Same diff also missed `ProfileStatsRow`'s own rating cell, which still falls back to the literal `'0.0 ★'` instead of the new "New seller" convention used one screen over (`seller_card.dart`, `vendor_store_screen.dart`, `vendor_store_card.dart`) for the identical missing-rating case.
- **Rule:** When a "stop fabricating X" fix targets one entity, grep the field name (`vendorRating`, `rating`, `salesCount`, etc.) across the WHOLE `lib/` tree before calling the fix done — the same fabricated-default pattern (`@Default(4.8)` / `?? 4.8` / `x == 0 ? fallback : x`) tends to be copy-pasted across every entity that models the same real-world concept (a vendor's rating shows up on product, cart, order, and profile screens, each with its own entity). Fixing the entity the bug report named is not the same as fixing the concept.
- **Where it applies:** `cart_entity.dart`, `cart_item_entity.dart`, `order_entity.dart`, `orders_remote_datasource.dart`, `cart_remote_datasource.dart`, `profile_stats_row.dart`; any future "don't fabricate/omit when missing" fix — check every entity carrying that field, not just the one in the original report.

### 2026-08-15 — Forcing MockConfig.useMock=false in release exposes every live-path getter that throws in kReleaseMode
- **What happened:** The same diff changed `MockConfig.useMock` from `_mockFromDefine` to `!kReleaseMode && _mockFromDefine`, closing the "mock ships to production" hole — but this also means release builds now unconditionally take the LIVE path for every feature gated on `MockConfig.useMock`, including `deliveryRequestDataSourceProvider` → `deliveryDioProvider` → `DeliveryApiEndpoints.baseUrl`, which itself correctly `throw StateError(...)` in `kReleaseMode` when `DELIVERY_API_BASE_URL` wasn't passed at build time. No build script (Taskfile, CI workflow) currently sets that define, so a release build now crashes the instant any delivery screen is opened — a crash the previous (mock-can-stay-true-in-release) behavior had been silently masking.
- **Rule:** A `kReleaseMode`-gated behavior change to a global flag like `MockConfig.useMock` is not a local fix — grep every consumer for a "live path that can throw in release" (a `baseUrl` getter with a `StateError`/`assert` guard is the tell) and confirm the CI/build pipeline actually supplies whatever define that guard requires, BEFORE forcing more code down that path. Fixing "mock leaks into prod" and "release build has no valid API host" are two different bugs that must be closed together, not one at a time.
- **Where it applies:** `mock_config.dart` `useMock`, `delivery_dio_provider.dart`, `delivery_api_endpoints.dart`; any future provider gated on `MockConfig.useMock` whose live datasource reads a `--dart-define`-sourced URL.

### 2026-08-15 — Egyptian E.164 phone digit-count checks: local number is 11 digits, E.164 digits (no `+`) is 12
- **What happened:** `whatsAppDigits` (new in this diff) validates with `if (digits.length < 11) return null;` after stripping the `+` from `AppValidators.toE164Egypt(raw)` — but a well-formed Egyptian E.164 number is always `+20` + 10 local digits = **12** digits once the `+` is stripped (the local form is 11 digits, `01XXXXXXXXX`). A malformed 9-digit input that `normalizeEgyptLocal`'s leading-0/leading-1 correction heuristics don't catch survives as an 11-digit "digits" string and passes the `< 11` check, so `launchWhatsApp` opens `wa.me` on a wrong/nonexistent number instead of hitting the intended unavailable-fallback path.
- **Rule:** Any digit-count validation downstream of `AppValidators.toE164Egypt`/`normalizeEgyptLocal` must know which stage it's checking: local form (`RegExp(r'^\d{11}$')`, matches the pattern already used in `Validators.egyptPhone`) vs. E.164-digits-without-plus (must be exactly 12, not "at least 11"). Use `==`, not `<`, for a fixed-length phone format — a lower-bound check silently accepts short/malformed numbers.
- **Where it applies:** `lib/shared/utils/whatsapp.dart` `whatsAppDigits`; any future phone-digit validation added alongside `AppValidators`.

### 2026-08-15 — Removing a hardcoded mock default (e.g. stock=99) can silently regress to a worse default if the real field was never wired into the mock data
- **What happened:** Fixing "don't invent stock" replaced `stockQuantity: 99` with `stockQuantity: m.stockQuantity` in `product_remote_datasource.dart`'s mock branch — correct in principle (stop fabricating), but `m` comes from `mock_listings.dart`'s `_CatalogRow`, whose `toListingModel()` never sets `stockQuantity` at all, so it falls through to `ListingModel`'s `@Default(0)`. Every mock product now reports stock 0 instead of a plausible in-stock number, capping the quantity selector at 1 and hiding the low-stock badge for the entire mock catalog — a dev/QA/demo regression, not a production one (live API responses do carry real `stockQuantity`).
- **Rule:** When replacing a hardcoded fake default with "read the real field," check that the field is actually POPULATED on every code path feeding that read — including mock/seed data construction, not just the live-API parsing path. A mock catalog that predates the field will silently supply its zero-value default and look like a data bug rather than the intended fix.
- **Where it applies:** `product_remote_datasource.dart` mock branches, `mock_listings.dart` `_CatalogRow.toListingModel()`; any future "stop fabricating, read the real field" fix — verify the mock data source was backfilled with that field, not just the live parser.

### 2026-08-15 — Deleting a stub screen and redirecting its routes can strand action-based deep links that point at the same route name
- **What happened:** `ComingSoonScreen` was deleted and its routes (`AppRoutes.earnings`, `AppRoutes.analytics`, etc.) now `redirect` to real destinations (mostly `/profile`) in `app_router.dart` — correctly closing the "tappable dead screen" class of bug for every in-app button/menu tile pointing at those routes. But `mock_notifications.dart` still sets `actionRoute: AppRoutes.earnings` on two notifications ("Payment Received", "Payout processed") whose COPY explicitly promises earnings content — the redirect silently lands the user on Profile with no acknowledgment of what they tapped for.
- **Rule:** When retiring a stub screen's routes via router-level redirect, grep every OTHER place a route constant is referenced as a target — not just widget `onTap`/`context.push` call sites, but also data-driven navigation like notification `actionRoute` fields, deep-link tables, or push-payload routing — and either update the copy to match the redirect's real destination or point the action at something that actually exists.
- **Where it applies:** `app_router.dart` route redirects, `mock_notifications.dart` `actionRoute` fields; any future stub-screen removal — grep the route constant, don't just grep the screen class name.

### 2026-08-15 — Listing category picker reads nested `children` from GET /api/categories
- **What happened:** The listing category sheet was a dumb list that callers could feed any taxonomy; the datasource flattened `/api/categories` into a parentId list, so subcategories depended on flattening instead of the nested `children` the live API actually returns.
- **Rule:** `GET /api/categories` is a bare list of top-level categories; subcategories are that object's `children` array (they also carry `parentId`). Keep the tree on `CatalogCategoryEntity.children` and drive the subcategory picker from it — do not flatten-then-filter. The picker sheet watches `allCatalogCategoriesProvider` itself so every call site hits the same endpoint.
- **Where it applies:** `catalog_category_remote_datasource.dart`, `catalog_category_model.dart`, `category_picker_sheet.dart`, `catalog_category_tree.dart`, add-listing category/subcategory fields.

### 2026-08-15 — Wiring an API taxonomy is not done until the hardcoded copy is deleted
- **What happened:** After the listing picker started reading `/api/categories`, the old slug list (`electronics`/`phones`/…), per-category l10n keys, and unused mock subcategory map were still in the app, so names could still resolve from local data instead of `nameEn`/`nameAr`.
- **Rule:** When a reference list moves to the backend, delete the static options, slug→label maps, and matching arb keys in the same change — keep only values the API does not own (here: listing condition tokens and brand suggestions). Names display from the API bilingual fields.
- **Where it applies:** `listing_categories_data.dart`, `listing_localized_labels.dart`, `lib/l10n/app_*.arb`, `mock_categories.dart`; any future "replace hardcoded list with GET /api/…" swap.

### 2026-08-15 — Shell-tab catalog pickers: open immediately, `.when` the async, tight Expanded list
- **What happened:** The listing category sheet went through three broken shapes: treating `AsyncLoading` as the item list; `Flexible` + `Column(mainAxisSize: min)` laying out the list at height 0; then awaiting `provider.future` on the *screen tap* before `showModalBottomSheet`, so a failed/in-flight fetch looked like a dead button (`catch` + `ScaffoldMessenger.maybeOf` can also swallow the snackbar on a shell tab).
- **Rule:** Open the sheet on tap immediately. Inside it, `ref.watch` + `AsyncValue.when` (spinner / error+retry that `invalidate`s / data). Give the list a tight `SizedBox(height: sheetContext.size * 0.55)` + `Expanded(ListView)` — never `Flexible` in a min-sized Column. `useRootNavigator: true` on shell tabs; `Navigator.pop` the sheet body context (not the ListTile's). Do not await the fetch on the caller, and do not treat `AsyncLoading` as items. Widget-test the Add Listing field tap through a nested navigator + bottom bar, including delayed and error providers.
- **Where it applies:** `category_picker_sheet.dart`, `add_listing_screen.dart` category/subcategory fields; any list picker opened from a `StatefulShellBranch` tab.

### 2026-08-15 — Commenting out a feature's CTA can strand its imports
- **What happened:** Deferring package delivery (Flow B) to phase 2 meant commenting out its UI entry points — the courier login button, two "My Packages" profile menu tiles, and the vendor order detail "Request custom delivery" card. The last one was the file's only reference to `send_package_screen.dart` (for `SendPackageArgs`) and its only `context.push`/navigation call, so both that import and `package:go_router/go_router.dart` became unused once the block was commented — `flutter analyze` caught the first pass (unused `send_package_screen.dart`/`app_routes.dart`) but a second pass was needed to catch `go_router` itself, since it's used via extension methods (`context.push`) rather than a named class reference that's easy to eyeball as "still used."
- **Rule:** After commenting out any widget block for a deferred feature, `flutter analyze` the file (imports are not caught by re-reading the diff) — and re-run it after each import removal, since removing one unused import can reveal another (an extension-method import like `go_router` has no textual trace once its only call site is commented).
- **Where it applies:** Any "comment out for phase N" change; especially files whose only use of a package is via extension methods (`context.push`, `context.l10n`, etc.) rather than an explicit type name.

### 2026-08-15 — Commission config exists on the backend now, but only as an admin-only endpoint; vendors only get an echo
- **What happened:** User supplied `PUT /api/admin/system-settings` body `{commissionValueOnOrder: 10, warnThresholdEgp: 100, pauseThresholdEgp: 200}` from a fresh Postman collection. Grepping the actual backend repo (`~/repos/xStoreEcommerce`, confirmed up to date on `origin/master`) found `SystemSettingsController` is `[Authorize(Roles = "ADMINISTRATOR, SUPERADMIN")]` — a vendor session can never call it. The vendor's actual owed balance (`VendorCommissionWallet.OutstandingEgp`) is *also* admin-only, exposed only via `UsersController` (`GET /api/users`, `PUT /{id}/commission/settle`), same role restriction — there is no vendor-facing "get my wallet" endpoint at all. The only vendor-accessible echo of any of this is `GetVendorOrdersQueryHandler` (`GET /api/vendor/orders`), which embeds `commissionValueOnOrder`/`warnThresholdEgp`/`pauseThresholdEgp` (straight from `SystemSetting`) plus `exceedsWarnThreshold`/`exceedsPauseThreshold` (booleans derived from the wallet's real `OutstandingEgp`, but never the number itself) in its response envelope — confirmed by reading the handler source directly, not by guessing from a DTO name.
- **Rule:** When a user hands over a Postman body for what looks like a new backend feature, don't assume the mobile app can call that literal endpoint — grep the actual backend repo's controller for its `[Authorize(Roles = ...)]` attribute before wiring anything. If the endpoint is role-restricted away from every app role, look for whether the same data is echoed on an endpoint the app's role *can* reach (here: vendor-orders already carried it) before concluding the feature is unreachable. When the backend only exposes a derived boolean (not the raw figure) to a role, stop displaying a client-computed version of the raw figure — a locally-summed "outstanding balance" can never re-sync after an admin-side settlement the client has no way to observe, so continuing to show it is worse than the boolean-only UI.
- **Where it applies:** `commission_config_provider.dart` (`vendorCommissionSnapshot`, sourced from `getVendorOrderStatsUseCaseProvider` instead of a new admin call), `vendor_commission_wallet_provider.dart`, `vendor_commission_wallet.dart` (now boolean-driven, no `outstandingEgp` field), `vendor_commission_alert_banner.dart` + arb strings (dropped the `{amount}` placeholder — never fabricate a money figure the backend won't give the current role). Any future "wire this admin endpoint into the mobile app" ask — check the role attribute first.

### 2026-08-15 — pickMultiImage(limit:) throws below 2 and is not a cap
- **What happened:** Switching listing gallery pick from `pickImage` (one at a time) to `pickMultiImage` so vendors can fill up to 5 photos in one shot. Passing `limit: remaining` when only 1 slot is left throws `ArgumentError` (`limit` cannot be lower than 2). On Android below API 33 the limit is also ignored, so the picker can return more files than remaining slots.
- **Rule:** Call `pickMultiImage(limit: remaining)` only when remaining ≥ 2; fall back to `pickImage` for a single slot. After the picker returns, always `take(remaining)` and clamp again in `addPhotoPaths` — never treat the OS `limit` as the business cap.
- **Where it applies:** `listing_form_notifier.dart` `pickFromGallery` / `addPhotoPaths`; any future multi-image pick with a numeric cap.

### 2026-08-22 — Register government/city are two API pickers; both IDs go on the wire
- **What happened:** Register needed government and city to load from the live lists on open, and to persist both ids on signup. A single cascade field hid the city picker, and vendor register only sent `storeCityId`/`storeGovernorateId`.
- **Rule:** Location pickers are two independent fields: government opens `allGovernmentsProvider` (`GET /api/governorates`), city opens `allCitiesProvider` (`GET /api/cities`) filtered client-side by `governorateId` (the cities endpoint ignores filter query params). Save `storeCityId`/`storeGovernmentId` in register state. Consumer POST sends `cityId` + `governmentId` (plus `governorateId` alias). Vendor multipart sends those two *in addition to* `storeCityId`/`storeGovernorateId`. Live seed currently has cities only for Cairo/Alexandria/Giza.
- **Where it applies:** `location_cascade_field.dart`, `register_screen.dart` step 2, `auth_remote_datasource.dart` `registerConsumer`/`registerVendor`.

### 2026-08-23 — Merged cascade tile is fine as long as the city sheet is unconditionally reached and wire IDs are untouched
- **What happened:** Product explicitly asked for `LocationCascadeField` to go back to one tappable field: tap → governorate sheet → picking a governorate auto-opens the city sheet (filtered to it) → picking a city closes both and renders `"Governorate - City"`. This looks like a revert of the 2026-08-22 "two independent pickers" fix, whose actual bug was that the old single-field version left the city picker unreachable (no cascade fired, no separate affordance either) so `cityId` never got set.
- **Rule:** The 2026-08-22 fix was about the city sheet being unreachable, not about tile count. A merged tile is safe again ONLY if every tap path unconditionally reaches the city sheet (no early return that skips it once a governorate is picked) and the `onChanged(cityId, governmentId)` contract / wire plumbing (`storeCityId`/`storeGovernmentId`, consumer `cityId`+`governmentId`, vendor multipart) is left untouched — verify both before collapsing tiles again. Dismissing the auto-opened city sheet after a governorate CHANGE must clear the stale city id; dismissing it with the governorate UNCHANGED keeps the previously picked city.
- **Where it applies:** `location_cascade_field.dart`, any future redesign of the register/edit-profile location picker.

### 2026-08-23 — Bottom-sheet "back" needs a third pop value, not just id/null
- **What happened:** Added a back button to the city sheet so users can reopen the governorate sheet without losing progress. `showModalBottomSheet<int>` only had two outcomes (`int` picked, `null` dismissed) — a back tap needs to be distinguishable from both, or it either closes the whole flow (mistaken for "dismissed") or gets misread as picking id `null`.
- **Rule:** When a chained bottom sheet needs a "go back" affordance, widen the sheet's pop type (`showModalBottomSheet<Object>`) and pop a private `static const Object _goBack = Object()` sentinel from the back button; drive the chain with a `while (true)` loop that `continue`s on the sentinel and `return`s on `null`/success. Never overload the same nullable type to mean both "dismissed" and "go back."
- **Where it applies:** `location_cascade_field.dart`; any future multi-step `showModalBottomSheet` flow needing in-flow back navigation.

### 2026-08-23 — Analytics collector POSTs only while signed in; dedicated Dio must attach X-Auth-Token itself
- **What happened:** `POST /api/analytics/events` was firing for guests. The dedicated telemetry Dio (kept separate so 5xx cannot trip the full-screen server-error interceptor) also never inherited `dio_provider`'s `X-Auth-Token` interceptor, so even a logged-in flush would have gone out without a session. `ref.listen(authProvider)` alone missed the already-resolved session at startup. An overlapping `_flush()` (init's unawaited flush vs `flushNow()`) returned immediately while the first POST was still in flight, so tests saw zero requests.
- **Rule:** Gate telemetry POSTs on a signed-in user (non-empty id) *and* a session token; skip the network for guests and queue locally until login. On a dedicated Dio, attach `X-Auth-Token` per request — do not share `TokenRefreshInterceptor` (a telemetry 401 must back off, not log the user out). Await `authProvider.future` after `listen` so the first flush sees the restored session, not just later changes. Coalesce in-flight flushes (`return` the same Future) so callers that `await flush` actually wait for the POST.
- **Where it applies:** `analytics_service.dart` `_flush` / `_runFlush` / `_sessionToken`; any future background collector that uses its own Dio.

### 2026-08-23 — iOS FCM getToken requires an APNS token first
- **What happened:** Cold-start `refreshAndStoreFcmToken()` called `FirebaseMessaging.getToken()` immediately after `requestPermission()`. On Apple platforms the plugin throws `firebase_messaging/apns-token-not-set` until APNS has delivered a device token (always on Simulator; briefly on a real device).
- **Rule:** On iOS/macOS, poll `getAPNSToken()` until it is non-empty before calling `getToken()`. If APNS never arrives, skip `getToken()` (do not catch-and-log the exception as the happy path). `onTokenRefresh` still persists/registers the token if it shows up later.
- **Where it applies:** `fcm_token.dart` `refreshAndStoreFcmToken` / `waitForApnsToken`; any future Apple-only FCM token fetch.

### 2026-08-23 — prefixIcon + 16px vertical padding clips InputDecoration.hintText
- **What happened:** AuthTextField set `hintText` but the placeholder looked missing. `prefixIcon` keeps the field at 48px tall; `contentPadding` vertical 16+16 left ~16px for text, while themed `bodyMedium` hint uses `height: 1.4` (~20px), so the glyphs were clipped.
- **Rule:** Filled outlined fields with a prefix/suffix icon must not use 16px vertical content padding — use the theme token (`AppSpacing.inputContentPaddingV` = 12). Set an explicit `hintStyle` with `height: 1` and `color: context.textHint` (same pattern as ListingFormField). Widget-test that the hint rect overlaps the field and its `AnimatedOpacity` is 1.
- **Where it applies:** `auth_text_field.dart` and any custom `InputDecoration` that sets both `prefixIcon`/`suffixIcon` and `contentPadding`.

### 2026-08-23 — find.text is not proof a TextField hint is visible
- **What happened:** Email and password placeholders still looked missing after padding/hintStyle tweaks. `find.text` and `AnimatedOpacity(1)` passed while the input slot was squeezed; PhoneInputField hints showed because of `isDense` + tight `prefixIconConstraints`.
- **Rule:** Copy a working sibling field's layout (`isDense: true`, `prefixIconConstraints` min ~40, no unconstrained `IconTheme` wrap). Assert hint **width** on a phone-sized surface with prefix+suffix. Use `textSecondary` for hint color — `textHint` on white is ~2.5:1. Hot reload often will not rebuild an existing `InputDecoration`; confirm with a full restart.
- **Where it applies:** `auth_text_field.dart`, `phone_input_field.dart`, any filled outlined field with `prefixIcon`.

### 2026-08-23 — AuthTextField placeholder is per call site, not the widget
- **What happened:** Email hint worked after `enterEmailHint` was passed; full name still looked empty because that `AuthTextField` never got a `hint`.
- **Rule:** `AuthTextField` does not invent a placeholder from `label`. When adding in-box copy, grep every `AuthTextField(` on the screen and pass `hint` on each empty one in the same change — do not wait for another field-by-field ping.
- **Where it applies:** Every `AuthTextField(` in `lib/features/auth/presentation/screens/`.

### 2026-08-23 — Don't put a real person's name in placeholder copy
- **What happened:** Full-name hint used "Ahmed Taha" (the developer's name); the user asked to change it to "Ahmed Mohamed".
- **Rule:** Placeholder examples are generic sample data, not the current user's or developer's real name. Prefer a conventional dummy like "Ahmed Mohamed".
- **Where it applies:** `fullNameHint` and any `e.g.` / `مثال:` hint strings in `lib/l10n/`.

### 2026-08-23 — Don't validate a field you hid
- **What happened:** Register Continue on step 2 did nothing: `validateStep` still required `fullNameAr` after the Arabic name field was commented out, so `nextStep` failed with an error that had no widget to show it.
- **Rule:** When a form field is removed or commented out, delete its `validateStep` entry in the same change. A `stepErrors` key with no matching `errorText:` looks like a dead Continue button. If the backend still wants the value, copy a visible field onto the wire (here: `fullName` → `fullNameAr`) instead of blocking the UI.
- **Where it applies:** `auth_provider.dart` `validateStep` / `_executeRegister`; any wizard whose UI fields and validator keys must stay 1:1. Recurred 2026-08-25 for `storeNameAr` and `storeDescriptionAr` — copy the visible field onto the wire instead of requiring a hidden bilingual field.

### 2026-08-23 — Location picker sheets search both nameEn and nameAr
- **What happened:** Government and city bottom sheets needed in-sheet search. Filtering only the displayed locale would miss mixed-language typing, which is common in Egypt.
- **Rule:** Client-side filter on bilingual reference lists matches against both `name.en` and `name.ar`. Own the search `TextEditingController` on a StatefulWidget inside the sheet and dispose it. Pad with `viewInsetsOf` so the keyboard does not cover the field. Do not autofocus the search field — a focused TextField's cursor blink hangs `pumpAndSettle`.
- **Where it applies:** `location_cascade_field.dart` lookup sheets; any future searchable reference-data bottom sheet.

### 2026-08-24 — Register/update-profile display name wire key is `fullName`
- **What happened:** The app still posted `fullNameEn` on consumer/vendor register and update-profile after the backend renamed that field to `fullName`.
- **Rule:** The live display-name key is `fullName` only — do not send `fullNameEn` or `fullNameAr` on register or update-profile. When reading profiles, accept `fullName`, then `fullNameEn`, then `name`.
- **Where it applies:** `auth_remote_datasource.dart` register bodies, `profile_remote_datasource.dart` `updateProfileWireFields`, `user_model.dart` `fromJson`.

### 2026-08-24 — Edit-profile location cascade needs register names as fallback
- **What happened:** Edit profile's location field showed the generic "Select government & city" hint after signup because names only resolved from live city/governorate lists, and get-profile city/government objects were not flattened onto `storeCity`/`storeWilaya`.
- **Rule:** Parse get-profile city/government from both `user` (consumers) and `store` (vendors), including nested `{id,nameEn,nameAr}` objects, onto `storeCityId`/`storeGovernmentId` and the name strings. On edit profile, pass those registered names as `LocationCascadeField.hint` so the field shows "Governorate - City" until list lookup resolves the ids. Do not mix GPS `town`/`governorate` into that hint — those are a different address pair.
- **Where it applies:** `user_model.dart` `parseProfileResponse` / `fromJson`, `location_cascade_field.dart`, `edit_profile_screen.dart`.

### 2026-08-24 — Location wire key is `governorateId`, not `governmentId`
- **What happened:** Register and update-profile posted `governmentId`; the backend field is `GovernorateId`, which serializes as `governorateId`.
- **Rule:** Write `cityId` + `governorateId` on consumer register, vendor register, and update-profile. Do not send `governmentId`. When reading profiles, accept `governorateId` first and fall back to `governmentId`. Vendor multipart still also sends `storeGovernorateId`.
- **Where it applies:** `auth_remote_datasource.dart`, `profile_remote_datasource.dart` `updateProfileWireFields`, `update_profile_request.dart`, `consumer_register_params.dart`.

### 2026-08-25 — Analytics ingest body is `{events: [...]}` not a bare array
- **What happened:** The live collector (`POST /api/analytics/events`, Postman "POST Ingest Events") binds an envelope `{ "events": [ ... ] }`. Sending a raw list or a single event object fails model binding.
- **Rule:** The analytics POST body is exactly `{ "events": <list of event objects> }` — never a top-level array and never one event as the root. Assert it on the Dio request `data`.
- **Where it applies:** `analytics_service.dart` `_runFlush`.

### 2026-08-25 — Analytics queue drops only the sent batch on 2xx
- **What happened:** Confirming buffer-clear behavior: a 200 (or any other 2xx) removes the batch that was POSTed; a 404 (collector not deployed) and thrown non-2xx leave the queue for retry. Unsent events beyond the batch of 20 stay queued.
- **Rule:** Treat any 2xx from `POST /api/analytics/events` as "this batch delivered — `removeRange` it and persist." Do not drop the rest of the queue. Do not drop on 404.
- **Where it applies:** `analytics_service.dart` `_runFlush`.

### 2026-08-25 — Vendor register store category uses GET /api/categories
- **What happened:** The vendor store-setup dropdown watched `allStoreCategoriesProvider` (`/api/storecategories`) while listings/home already used `allCatalogCategoriesProvider`.
- **Rule:** Vendor `storeCategoryId` is a catalog category id. Watch `allCatalogCategoriesProvider` (`GET /api/categories`) for that dropdown — do not fetch `/api/storecategories`. Show the top-level list as returned; do not flatten `children` into the register dropdown.
- **Where it applies:** `register_screen.dart` store-setup dropdown; any future store-category picker.

### 2026-08-26 — isEmailVerified/isPhoneNumberVerified: extend ProfileEntity, not UserEntity/UserModel, when there's no SDK to regenerate codegen
- **What happened:** Building the email/phone OTP verification flow in Edit Profile needed to surface `isEmailVerified`/`isPhoneNumberVerified` (sent by the backend alongside `isVerified` on the user object, per an existing-but-previously-unwired code comment in `user_model.dart`). `UserEntity`/`UserModel` are freezed classes with ~1000-line generated mirrors each (`user_entity.freezed.dart`, `user_model.freezed.dart`), and this session had no `flutter`/`dart` binary anywhere on disk to run `build_runner`. Adding two fields there would have meant hand-editing ~14 call sites across two giant generated files with no way to verify the result compiles. Instead, the two flags were added only to `ProfileEntity` (a 21-line source / ~360-line generated freezed file with a fraction of the fields) — architecturally justified since `ProfileEntity` already carries the sibling `isEmailVerificationRequired`/`isPhoneVerificationRequired` flags from the exact same wrapper response, parsed in the same `parseProfileResponse` function. `ProfileResponseWire` and `ProfileModel` (both hand-written, non-freezed classes) needed no codegen at all.
- **Rule:** When a new field must be threaded through a freezed entity and no SDK is available to regenerate, prefer the smallest freezed class that's architecturally defensible over the "obviously right" one — check whether a sibling small entity already carries data from the same response/call site before touching a large one. Reserve full generated-file hand-editing (mirroring every one of copyWith/==/hashCode/toString/factory across `mixin`, both `CopyWith` interfaces+impls, the concrete `Impl` class, and the abstract base) for when there's truly no smaller entity to extend, and re-read the whole generated file afterward to confirm every one of those ~7+ spots per added field was updated consistently.
- **Where it applies:** `profile_entity.dart`/`profile_entity.freezed.dart`, `user_model.dart` `ProfileResponseWire`/`parseProfileResponse`, `profile_model.dart`; any future field addition to a freezed entity in a session without a Flutter/Dart SDK.
- **Correction 2026-08-27:** The "sent alongside isVerified on the user object" premise above was wrong — see the same-day lesson below. The architectural conclusion (extend `ProfileEntity`, not `UserEntity`/`UserModel`) still held regardless, since `parseProfileResponse` has access to the full top-level `data` map either way.

### 2026-08-27 — isEmailVerified/isPhoneVerified are top-level siblings of `user`, not nested inside it — an inherited code comment was never actually verified
- **What happened:** The Edit Profile "Verified" badge stayed unchecked after a real, successful email verification. User pasted the ACTUAL live `GET /api/auth/get-profile` response: `{"user": {...}, "store": null, "isEmailVerified": true, "isPhoneVerified": false}` — both flags are top-level, exactly like the already-correctly-handled `isEmailVerificationRequired`/`isPhoneVerificationRequired`. But `parseProfileResponse` read them as `userJson['isEmailVerified']`/`userJson['isPhoneVerified']` (i.e. from inside the nested `user` object), because that's where a pre-existing code comment in `UserModel.fromJson` (from a previous session, never re-verified) claimed they lived: "real response sends isEmailVerified/isPhoneVerified alongside isVerified". Since real responses never put them there, the read was always `null` → always defaulted to `false`, regardless of the actual verification state. This was a completely different bug from the same-day force-refresh race condition fixed just before it — that fix was real and necessary, but insufficient on its own, because even a guaranteed-fresh fetch was still reading the flag from the wrong place.
- **Rule:** An inherited code comment asserting a backend field's location/shape is a claim from whenever it was written, not a verified-today fact — treat it exactly like a "CONFIRMED via live probe" lesson that can go stale (see the 2026-08-02 lesson on that). When a flag consistently reads as its default/false despite the action that should flip it succeeding, suspect the parse location before adding more guards/fixes around the read site. A real captured response body (pasted by the user, a debug log, a probe) always outranks an inherited comment — re-verify the comment's claim against it immediately rather than assuming the bug is elsewhere.
- **Where it applies:** `user_model.dart` `parseProfileResponse` (now reads both flags from `data`, matching the `...VerificationRequired` pair); any future debugging of a boolean flag that "never updates" — check the parse location, not just the refresh timing/caching.

### 2026-08-26 — A new GoRoute reading `state.extra` must nullable-cast + redirect, not force-cast
- **What happened:** The new `AppRoutes.profileVerification` route's `pageBuilder` did `ProfileVerificationScreen(args: state.extra as ProfileVerificationArgs)` — a non-nullable force-cast. Every other `extra`-carrying route in `app_router.dart` (`resetPassword`: `state.extra as String? ?? ''`; `sendPackage`: `state.extra as SendPackageArgs?`) casts nullable with a fallback, because `extra` is never serialized — a cold deep link, hot-restart, or route-restoration replay reaches the route with `extra == null` and the force-cast throws `type 'Null' is not a subtype of...`, crashing the app. Caught by a dedicated audit subagent, not by self-review.
- **Rule:** Any `GoRoute` that reads `state.extra` for a required, non-nullable constructor argument needs a `redirect:` guard that checks `state.extra is! ExpectedType` and redirects elsewhere BEFORE `pageBuilder` runs (mirrors the existing `AppRoutes.otp` route's `redirect` pattern checking `phoneAuthProvider` state) — never force-cast a required non-nullable arg directly in `pageBuilder`. If the argument can reasonably be optional instead, follow `sendPackage`'s pattern (nullable cast, nullable constructor param) instead of adding a redirect.
- **Where it applies:** `app_router.dart`; any future route taking a required non-primitive `extra` payload.

### 2026-08-26 — Calling a StateNotifier method directly from initState() crashes if its first line writes `state =`
- **What happened:** User reported the OTP resend timer on the new profile-verification screen "isn't working" — the real symptom was a runtime crash the user hit immediately on opening the screen: `Tried to modify a provider while the widget tree was building` / `At least one listener of the StateNotifier ... threw an exception`. Root cause: `ProfileVerificationScreen.initState()` called `ref.read(profileVerificationProvider(widget.args).notifier).sendCode()` directly. `sendCode()`'s very first statement is `state = state.copyWith(isSending: true, clearError: true);` — a synchronous state write executed before any `await`, and Riverpod's `_debugCanModifyProvider` guard explicitly forbids modifying a provider's state during `initState`/`build`/`dispose`/`didUpdateWidget`/`didChangeDependencies`. The screen threw before the OTP send (and therefore the resend cooldown, which only starts on send success) ever ran — so "the timer isn't working" was actually "the screen crashed and nothing happened." This was NOT caught by an earlier static/symbol-tracing audit of the same file, because it's a Riverpod *runtime lifecycle* rule, not a type error — confirms the standing CLAUDE.md instruction that a manual code read is not a substitute for actually running the app.
- **Rule:** Never call a StateNotifier/Notifier method directly from `initState()` (or `build`/`dispose`/etc.) if that method's *first* action — before any `await` — writes to `state`. This is a stricter trap than the already-documented "side-effect launched from build()" lesson: it doesn't matter whether the method reads another provider, ANY synchronous `state = ...` inside a method called from a widget lifecycle method trips Riverpod's guard immediately, even on the very first line. Defer with `Future(() { if (!mounted) return; ref.read(...).notifier.methodName(); })` — matches the existing auth-prefetch pattern already documented above. A local widget `setState()` (not a Riverpod provider write) called synchronously in `initState()` is fine and does NOT need this treatment — only Riverpod provider state writes are guarded this way.
- **Where it applies:** `profile_verification_screen.dart` `initState()`; any future screen that kicks off a send/fetch by calling a notifier method directly from `initState()`.

### 2026-08-27 — Delete-account was calling a legacy route, and two backend-driven OTP flows were fully built but never wired to any screen
- **What happened:** A fresh Postman collection audit found `ProfileRemoteDataSource.deleteAccount()` called `DELETE /users/me` (its own comment admitted "NOT in the confirmed backend contract") while the collection has a real, confirmed `DELETE /api/auth/delete-account` requiring a JSON body `{password, confirmationText}` — the app's delete-account dialog only ever collected the confirmation keyword, never a password, so even pointing at the right route would have 400'd. Separately, `sendEmailOtpUseCase`/`verifyEmailOtpUseCase`/`sendPhoneOtpBackendUseCase`/`verifyPhoneOtpBackendUseCase` (and their `AuthRepository` methods) were fully implemented with working live routes, `ProfileEntity.isEmailVerificationRequired`/`isPhoneVerificationRequired` were correctly parsed from `get-profile` onto `ProfileState`, and `phone_verification_sheet.dart`'s `verifyPhoneNow()` sheet already existed — but nothing in `profile_screen.dart` ever read those two flags or called that sheet, so a user whose email/phone `get-profile` marked unverified saw no indication of it anywhere in Profile (checkout's phone-verification gate, added 2026-08-14, is a separate call path and doesn't cover email or a profile-initiated check).
- **Rule:** When a Postman collection confirms a route the app calls differently, don't just swap the route string — read the confirmed request body/headers too (a right route with a wrong body still fails). When auditing "is X implemented," a usecase/provider/repository chain existing and compiling is not proof it's reachable — grep the usecase provider's construction across `lib/` for real call sites (same check as the 2026-08-04 "trace before flagging" lesson) before concluding a documented-as-live backend capability is actually surfaced to the user.
- **Where it applies:** `api_endpoints.dart` (`deleteAccount`), `profile_remote_datasource.dart`/`profile_repository.dart`/`delete_account_usecase.dart`/`delete_account_dialog.dart` (password + confirmationText threaded end to end), `profile_screen.dart` + new `profile_verification_banner.dart` + new `email_verification_sheet.dart` (mirrors `phone_verification_sheet.dart`) — any future "is this endpoint wired" audit.

### 2026-08-27 — Field-level actions go in suffixIcon, not a row below
- **What happened:** Email/phone "Verify" on edit profile sat in a `_VerificationStatusRow` under each field. Default `TextButton` min size (~48px) made it look like its own line instead of part of the input.
- **Rule:** Inline field actions (verify, status badges) belong as a compact `suffixIcon` with `suffixIconConstraints: BoxConstraints(minWidth: 0, minHeight: 0)` and a shrink-wrapped `TextButton`. Don't place them in a separate widget below the field. Shared inputs like `PhoneInputField` take an optional `suffix` rather than wrapping the whole control in a `Row`.
- **Where it applies:** `edit_profile_screen.dart`, `phone_input_field.dart`; any TextField trailing action.

### 2026-08-27 — "Don't invent seller stats" left four more entities unfixed for over a month
- **What happened:** A release-readiness audit re-confirmed a gap the 2026-08-15 lesson above had already named but never closed: `WishlistItemEntity.rating` (`@Default(4.7)`), `CartVendorGroup.vendorRating`/`CartItemEntity.vendorRating` (`@Default(4.8)`), `OrderEntity.vendorRating` (`@Default(4.8)`), and the separate data-layer `OrderModel.vendorRating` (`@Default(4.8)`) were all still non-nullable with a fabricated default, fed by `rating == 0 ? 4.7 : rating` / `?? 4.8` in their datasources, and rendered unconditionally as a star+number in `wishlist_item_card.dart`, `wishlist_grid_card.dart`, `cart_vendor_group.dart`, and `order_detail_scroll_content.dart`. Fixed by making all four fields `double?` (no default), treating missing-or-zero as "no rating yet" in every parser, and falling back to `context.l10n.newSeller` / `noReviewsYet` in the UI instead of a number. Mock-mode fixtures (`_ensureMockSeed`, `_vendorDisplay`, `mockVendorUser.rating ?? 4.8` in orders) were deliberately left alone — realistic demo data in a mock-only code path isn't the same violation as fabricating a number for a real user with no real rating.
- **Rule:** When a "stop fabricating X" lesson names entities to check, treat that as a checklist to verify closed, not a one-time fix — re-grep the field name across the whole `lib/` tree on the NEXT related task, not just when the original bug is reported again. A shared concept (seller/vendor rating) recurring across wishlist/cart/order entities means the same default-value anti-pattern gets copy-pasted into each one independently; fixing it requires touching every entity, every datasource parse site, and every unconditional render site in one pass, or the lesson just gets rediscovered later.
- **Where it applies:** `wishlist_item_entity.dart`, `cart_entity.dart`, `cart_item_entity.dart`, `order_entity.dart`, `order_model.dart` and their freezed mirrors; `wishlist_remote_datasource.dart`, `cart_remote_datasource.dart`, `orders_remote_datasource.dart`; any future entity carrying a rating/sales/stat field sourced from an API that may omit it.

### 2026-08-27 — `refreshProfileData(force: true)` coalesced into a stale in-flight fetch, hiding just-completed verification
- **What happened:** User reported that after verifying their email, the "Verified" badge on Edit Profile never updated. `ProfileVerificationScreen._handleVerified()` correctly awaits `refreshProfileData(force: true)` before popping — but `refreshProfileData`'s in-flight-coalescing check (`if (existing != null) return existing;`, added by an earlier lesson to dedupe concurrent tab-open/prefetch calls) ran unconditionally, ignoring `force`. If any other refresh was already in flight when verification completed (e.g. triggered by the Profile tab mounting moments earlier), the forced call just awaited that OLDER future and returned data fetched *before* the email was verified — `state.profile.isEmailVerified` stayed false even though the OTP verify itself had succeeded. Intermittent by nature (only reproduces when a refresh happens to already be in flight), which is why it wasn't caught by the earlier manual audit of the verification screen itself — the bug lived one layer down, in a provider method that screen didn't change.
- **Rule:** A "coalesce concurrent calls into one in-flight future" optimization must never apply to a caller that passed `force: true` — force means "I need this specific fetch's result, started now, not whatever was already running." Skip the coalescing check entirely when `force`, start a new fetch, and guard against the *old* in-flight fetch's result landing later and clobbering the fresh one — a monotonic per-fetch request id (bumped on every fetch actually started, compared at every point the fetch is about to write `state`) does this without needing to cancel the old future outright. This is a distinct hazard from the existing `_sessionEpoch` guard (which only protects across logout/session boundaries) — two overlapping fetches in the *same* session need their own ordering guard.
- **Where it applies:** `profile_provider.dart` `ProfileNotifier.refreshProfileData`/`_refreshProfileDataImpl`; any future `force: true`-style "must be fresh" refresh built on top of an in-flight-coalescing pattern.

### 2026-08-27 — Name profile verification flags after the live wire keys
- **What happened:** Edit Profile Verify/Verified was wired to `ProfileEntity.isPhoneNumberVerified` while the get-profile payload sends `isPhoneVerified`. The parser mapped the live key into the invented name, so a reader (or a later change) looking for `isPhoneVerified` on the profile would miss the phone badge.
- **Rule:** Keep Dart field names for profile wrapper flags 1:1 with the live JSON keys (`isEmailVerified`, `isPhoneVerified`). Accept an older alternate key only as a parse fallback, never as the domain/UI name.
- **Where it applies:** `parseProfileResponse`, `ProfileEntity`, `ProfileModel`, Edit Profile verify suffix, `requirePhoneVerified`.

### 2026-08-27 — `flutter test` alone hides half the auth/profile suite; run both CI defines locally
- **What happened:** Auditing the auth/profile modules with a plain `flutter test` showed "all green," but 8 of the tests carry `skip: MockConfig.useMock ? false : '...'` (or the inverse) and only run under `--dart-define=MOCK=true` — the exact second job CI runs (`ci.yml` "Run tests (MOCK=true, mock data paths)"). Running that second command surfaced three real, previously-invisible-locally failures: (1) `Auth.setUser`/`Auth.adoptSession` called `syncFcmDeviceTokenWithBackend(ref)`/`syncDeliveryBackendSession(ref)` with no `user` — see the corrected 2026-07-23 FCM lesson above; (2) `profile_prefetch_test.dart`'s "cold start with persisted session" test built a raw `ProviderContainer` without overriding `fcmDeviceTokenSyncProvider`/`fcmPushHandlingProvider`, so `Auth.build()`'s `ref.read(fcmDeviceTokenSyncProvider)` hit the real `FirebaseMessaging.instance` and threw `[core/no-app]` (no `Firebase.initializeApp()` in that test) — fixed by adding the same overrides the sibling "cold start without persisted session" test in the same file already uses; (3) `profile_update_payload_test.dart`'s `updateAvatar` multipart test asserted against a captured Dio request, but `ProfileRemoteDataSourceImpl.updateAvatar` short-circuits to `MockConfig.simulate(...)` before touching Dio when `MOCK=true` — it needed its own `skip: MockConfig.useMock ? '...' : false` guard (inverse of the usual pattern, since this test specifically exercises the real-network path).
- **Rule:** Before calling any auth/profile (or other MOCK-gated) test change done, run it under **both** `flutter test` and `flutter test --dart-define=MOCK=true` — matching `ci.yml` exactly. A test gated by `skip: MockConfig.useMock ? ... : ...` that only compiles/passes in the mode you happened to run is not verified. When a new `ProviderContainer`-based test reads `authProvider` on a cold-start/restore path (not `adoptSession`), it will trigger `Auth.build()`'s one-time `ref.read(fcmDeviceTokenSyncProvider)` — override `fcmDeviceTokenSyncProvider`/`fcmPushHandlingProvider` with no-ops unless the test specifically wants to exercise Firebase Messaging.
- **Where it applies:** All tests under `skip: MockConfig.useMock ...` in `test/features/auth/`, `test/features/profile/`, `test/auth_repository_impl_test.dart`; any new `ProviderContainer` test that reads `authProvider` cold (not via `adoptSession`).

### 2026-08-27 — `fcmDeviceTokenSync`'s unguarded `FirebaseMessaging.instance` call can take down auth session restore
- **What happened:** `Auth.build()`'s cold-start path does `ref.read(fcmDeviceTokenSyncProvider)` (unguarded) on every first restore. That provider's body called `FirebaseMessaging.instance.onTokenRefresh.listen(...)` with no try/catch — any synchronous throw there (proven locally via missing `Firebase.initializeApp()` in a test, but the same class of failure is real on Google-Play-Services-less Android devices, common in this app's Egypt/budget-device target market) propagates straight up through `ref.read`, out of `Auth.build()`, turning the entire `authProvider` into `AsyncError` — i.e. a supplementary push-notification feature failing takes the whole session-restore path down with it.
- **Rule:** Any provider read unconditionally from `Auth.build()`'s cold-start path (session restore is not optional/skippable for the user) must not let a failure in its own setup escape — wrap platform-channel calls in try/catch and log+no-op on failure, the same pattern already used in `requestFcmNotificationPermission()`. Never let a "nice to have" side effect (push token sync, analytics, delivery-backend bridge) be capable of failing the thing it's attached to.
- **Where it applies:** `fcm_device_token_sync_provider.dart` `fcmDeviceTokenSync`, and any other `keepAlive` provider read unconditionally from `Auth.build()`'s `firstRestore` branch.

### 2026-08-27 — Vendor mock `getProfile` backfills missing fields from `mockVendorUser`, including legacy `storeCity`
- **What happened:** `profile_update_payload_test.dart`'s save→fetch round-trip test asserted `u.storeCity` stayed `null` after an `updateProfile` call that never touched `storeCity` — but `ProfileRemoteDataSourceImpl._mergeVendorMock` (used by mock `getProfile`) does `storeCity: session.storeCity ?? base.storeCity` like it does for `location`/`storeName`/`storeSlug`/etc., and `mockVendorUser.storeCity` is hardcoded `'Cairo'`. The round trip correctly returned `'Cairo'`, not `null` — the test's assumption was wrong, not the mock.
- **Rule:** `_mergeVendorMock`/`_mergeConsumerMock` deliberately backfill any session field the request didn't set with `mockVendorUser`/`mockConsumerUser` demo data, so mock-mode profiles never show blank fields. A save→fetch round-trip test must only assert `isNull`/absence for a field none of `_mergeVendorMock`'s fallbacks cover — for anything in that merge function's field list, assert the expected mock-backfilled value instead (with a comment noting it's the mock fallback, not the request).
- **Where it applies:** `profile_remote_datasource.dart` `_mergeVendorMock`/`_mergeConsumerMock`, `mock_users.dart` `mockVendorUser`/`mockConsumerUser`, any new mock round-trip test in `profile_update_payload_test.dart`.

### 2026-08-27 — LoggingInterceptor had a debug-only exception that leaked the live session token, contradicting its own class doc and its own test
- **What happened:** A full-repo `flutter test` (both CI defines) run surfaced one real failure outside auth/profile/listing: `logging_interceptor_test.dart`'s `onRequest redacts Authorization header and sensitive body fields` asserted the request log must never contain the `X-Auth-Token` value, but `LoggingInterceptor._redact` had `final skipRedact = kDebugMode && keyLower == 'x-auth-token';` — deliberately printing the live per-user session token to the console on every debug-build request, "for backend verification." That directly contradicts the class's own doc comment ("Redacts auth headers ... so secrets never end up in device logs") and the test that was written to enforce exactly that promise — i.e. the code drifted away from both its own contract and its own test coverage without anyone re-running the suite.
- **Rule:** When a security-relevant redaction/masking function has a narrow "just for debugging" exception, treat that as a smell, not a convenience — check it against the function's own doc comment and existing tests before assuming it's intentional-and-fine. Session/auth tokens never get a debug-mode carve-out in logging, even locally: device logs are exactly the kind of place (logcat, Xcode console, bug-report attachments, screen recordings) a leaked token becomes a real credential exposure. If a specific header/field genuinely needs visibility for a debugging session, do it as a one-off temporary `debugPrint` at the call site, never as a standing exception in shared logging infra every request flows through.
- **Where it applies:** `logging_interceptor.dart` `_redact`, and any future redaction/masking utility (analytics, crash reporting, structured logs) — no debug-mode exceptions for `_redactedHeaderKeys`/`_redactedBodyKeys`-class secrets. Also a process note: run the FULL suite (`flutter test` + `--dart-define=MOCK=true`), not just the module you're touching, before calling a change/session done — this bug lived outside auth/profile/listing entirely and was only caught by testing everything.

### 2026-08-28 — Store name and description are single fields, not bilingual
- **What happened:** Backend dropped `storeNameEn`/`storeNameAr` and `storeDescriptionEn`/`storeDescriptionAr` in favor of `storeName` and `storeDescription`. The app still modeled, validated, and posted the En/Ar pairs (register copied the visible field onto both keys; edit profile still showed Arabic fields).
- **Rule:** Vendor store name/description are one field each on the domain, the DTO, and the wire (`storeName`, `storeDescription`). Do not send or store En/Ar variants. Parse GET `store.name`/`store.description` first; keep En/Ar only as read fallbacks for old payloads, and skip blank strings so they do not block the fallback. Full name stays bilingual (`fullName` / `fullNameAr`).
- **Where it applies:** `VendorRegisterParams`, `UpdateProfileRequest`, `UserEntity`/`UserModel`, register/update-profile datasources, edit-profile form. Supersedes the 2026-07-11 bilingual store-name and 2026-07-25 bilingual store-description lessons, and the 2026-08-25 "copy visible field onto storeNameAr" workaround.

### 2026-08-28 — Backend media URLs keep the trial host from upload time; rewrite at image load
- **What happened:** A just-uploaded avatar 404'd in `CachedNetworkImageProvider` at `http://xstoreegy-001-site1.jtempurl.com/uploads/avatars/<id>.jpg`. Live probe: that old host 404s; the same path on current `ApiEndpoints.baseUrl` (`https://xstoreegy002-001-site1.etempurl.com`) returns 200 with the Basic license header and 401 without it. The backend stores absolute URLs using whatever tempurl host was configured at upload time, and those hosts rotate (see 2026-08-04). A Flutter image-resource dump is not "file missing" until the same path is tried on the current API origin.
- **Rule:** Resolve backend-hosted media at image-load time (`resolveBackendMediaUrl` in `AppCachedNetworkImage` / `AppNetworkImage`), not by mutating stored `avatarUrl` fields. Rewrite relative `/uploads/…` paths, `/uploads/` URLs on a stale origin, and `*.jtempurl.com`/`*.etempurl.com`/`*.tempurl.com` hosts onto `ApiEndpoints.baseUrl`. Leave Google/Facebook/picsum URLs alone. Always send `AppNetworkImageHeaders` (Basic license) on those fetches. Pass `errorListener` on `CachedNetworkImage`/`CachedNetworkImageProvider` so a genuine 404 still shows `errorWidget` without dumping `HttpExceptionWithStatus` through Flutter's image error service.
- **Where it applies:** `media_url.dart`, `app_cached_network_image.dart`; any future raw `CachedNetworkImage`/`NetworkImage` call site that bypasses those helpers.

### 2026-08-28 — A provider that listens to auth must not be read from Auth's own ref
- **What happened:** Delete-account (and any `Auth.logout` after analytics had initialized) threw `CircularDependencyError` in debug. `analyticsServiceProvider` did `ref.listen(authProvider)` (and `read(authProvider.future)`) so it depended on auth; `Auth.logout` then `ref.read(analyticsServiceProvider)` to track the event. Riverpod's debug assert walks the target's ancestors and throws when it finds the reader. `ProfileNotifier.deleteAccount` calling `ref.read(authProvider.notifier).logout()` was a second copy of the same shape: Profile → Auth, then Auth invalidates Profile / reads analytics.
- **Rule:** Session identity flows one way: Auth pushes into listeners via an explicit method (`bindSession`, `user:` args), never the reverse `ref.listen(authProvider)` inside a provider that Auth later `ref.read`s. A notifier must not `ref.read` a sibling notifier to run teardown the sibling owns — return success and let the widget's `WidgetRef` call it (the logout sheet pattern). Timing tricks (`Future(() => ref.read(...))`) do not break this graph; see the 2026-07-23 FCM self-dependency lesson.
- **Where it applies:** `analytics_service.dart` (`bindSession`, no `authProvider` listen), `auth_provider.dart` (`_bindAnalyticsSession` / logout track before `invalidateSelf`), `profile_provider.dart` `deleteAccount` (returns `bool`; does not read `authProvider`), `profile_menu_blocks.dart` (widget calls `Auth.logout` after a successful delete).

