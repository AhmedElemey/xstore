/// Fixed `?category=` route tokens for the featured fashion banners. They are
/// identifiers, not copy — the same in every locale, so they live here rather
/// than in the `.arb` files.
const kCategoryQueryMens = 'mens_fashion';
const kCategoryQueryWomens = 'womens_fashion';
